═══════════════════════════════════════════════════════════════════════════
FABLE5-BACKLOG.txt — what to have Fable 5 build before access ends
═══════════════════════════════════════════════════════════════════════════
Written 2026-07-02 by a Fable 5 session with full context of the codebase
(it had just walked every screen, retired Classic search, and shipped the
annotation hint). Owner has ~5 days of Fable 5 access. This file is the
menu: each item is scoped so ONE session can land it end-to-end.

HOW TO RUN A SESSION (the protocol that has worked)
  1. Session reads CLAUDE.md first (always), then this file.
  2. Pick ONE item (or one Tier-1 + one small Tier-2). Resist bundling.
  3. VERIFY CURRENT STATE before building — plans and counts in docs drift;
     read the code ([[verify-current-state-not-plan]]).
  4. Implement → preview-verify on the real corpus (import the owner's
     .votbak via adb if real data helps — proven workflow, see the
     2026-07-02 CLAUDE.md entries) → full gates (vitest + coverage +
     typecheck + lint --max-warnings 0 + smoke:ci) → CLAUDE.md handoff
     entry → commit → push → gradle assembleDebug + adb install -r if the
     owner's phone is connected.
  5. Mark the item DONE here with a one-liner.

HARD CONSTRAINTS (owner policies — never violate)
  • NO AI features, NO API keys, NO LLM anything. NO auth/login. NO new
    network egress (the app is self-contained + offline; only existing
    egress: Garden images from GitHub, external links opened OUT).
  • Do NOT touch the welcome splash (UX6 — owner veto).
  • Hidden Manna stays out of every public index/search/home tile.
  • Data faithfulness: never author content that isn't on the live site;
    corpus edits need CORPUS_VERSION bump + check_balance.py.
  • ≤800-line app.jsx canary; 428-global lint gate (regenerate via
    npm run lint when window-globals change); pre-commit rebuilds bundles
    from the working tree — one logical change per commit.

ALREADY REJECTED / ADJUDICATED — do NOT re-litigate
  • PF3 IntersectionObserver windowing (dropped: risks the load-bearing
    scroll-restore/annotation layer; content-visibility landed instead).
  • PERF1 cold-boot restructuring (won't-do: risk > gain, no budget users).
  • D3 global strictNullChecks; D4 NavContext; D8 wholesale sheet-hook
    dissolve; PF2 minify bundle-a (react UMD reads top-level `this`).
  • UX6 welcome splash changes.

───────────────────────────────────────────────────────────────────────────
TIER 1 — HIGHEST LEVERAGE (do these first)
───────────────────────────────────────────────────────────────────────────

[1] MY NOTES FULL-TEXT SEARCH  (gap found in the 2026-07-02 screen walk)
    Bookmarks, Links, and Highlights index screens all have a search box;
    the Notes index (the richest store: bodies + anchor excerpts) has NONE.
    Add the same `.notes-index-search` input filtering All Notes + drilled
    notebook views over note.body + fullText + source label.
    Acceptance: typing "covenant" filters both tabs live; empty state for
    no matches; tests for the filter fn; visual parity with Bookmarks.
    Effort: S.  Files: NotesIndexScreen.jsx (+ small CSS reuse).

[2] EXPORT NOTES / NOTEBOOK AS TEXT (shareable study notes, zero egress)
    A "Share as text" action on a notebook (drilled header ⋯ or button)
    and on All Notes: compose a plain-text/markdown document (note anchors
    as quotes + bodies + source labels + dates), hand it to the existing
    share/save path (navigator.share / SAF text write — the journal
    recording save + export flows show the pattern). NO new formats in
    backup — this is a human-readable artifact, not a backup.
    Acceptance: a 3-note notebook exports a readable .md/.txt; verified on
    web preview; Android path compiles + emulator-verified if possible.
    Effort: M.  Files: NotesIndexScreen, a small utils/notes-export.js.

[3] "MY PROGRESS" DASHBOARD (all data already exists, pure UI)
    One screen unifying: Mark-as-Read percentages (per collection — the
    Settings rows already compute them), journal streak (JournalStats),
    counts of notes/highlights/bookmarks, most-annotated books/letters
    (AnnotationStore keys → group by book/letter, resolve titles the way
    noteSourceLabel does). Entry point: Library card or Home tile.
    Acceptance: renders offline from stores; zero new persistence;
    respects historyEnabled=false (hide history-derived rows).
    Effort: M.  Files: new screen + route + Library/Home entry.

[4] BACKUP FRESHNESS REMINDER (the only-backup story needs this)
    The app's ONLY backup is manual Export. Track lastExportAt (StateStore
    write in the export success path — both platforms), and at boot show
    the existing toast pattern if >30 days AND user data is non-trivial
    (>50 KB): "It's been a while since your last backup — Export from
    Settings." One-tap deep link to Settings; a settings toggle to disable
    the reminder. NO scheduling APIs, NO notifications — boot-check only.
    Acceptance: toast fires on a stale-stamped profile, not on fresh
    export; toggle silences; tests around the threshold fn.
    Effort: S-M.  Files: SettingsScreen export paths, use-app-shell-
    effects (or App boot effect), use-settings default.

[5] SMOKE SEARCH RESULT ASSERTION (deferred v1.1 item, now MiniSearch-only)
    tools/smoke.js "Search" step still only asserts the screen renders.
    Make it type a known query (e.g. "shepherd") and assert ≥1 result card
    + ≥1 <mark> — guarding the whole engine+UI path in CI forever.
    Watch: cold index build in headless CI (~10s budget; the corpus is
    already loaded by earlier smoke steps, and the golden suite proves the
    engine, so assert with a generous timeout).
    Acceptance: smoke:ci green locally + in CI with the new assertion.
    Effort: S.  Files: tools/smoke.js (+ SMOKE.md note).

───────────────────────────────────────────────────────────────────────────
TIER 2 — UX / DESIGN CONTINUATIONS (the 2026-07-02 pass's natural sequels)
───────────────────────────────────────────────────────────────────────────

[6] JOURNAL BLOCK REORDER (drag handles)
    Blocks can be added/deleted but not reordered — a long entry with a
    misplaced quote must be rebuilt. The app already has TWO proven
    imperative drag implementations (HomeScreen 1D, TabsOverview 2D FLIP);
    reuse that exact pattern (long-press handle → FLIP siblings → commit
    via a JournalStore blocks splice). RED-first tests on the reorder fn.
    Effort: M-L (gesture code is subtle — copy the TabsOverview idioms).

[7] TAB NAMES + PIN
    Tabs remember titles now (useTabTitleMemo); let the ⋮ TabActionSheet
    add "Rename tab…" (persisted custom title overrides the memo) and
    "Pin" (pinned tabs sort first + skip Close-others/Clear-all).
    Effort: M.  Files: TabActionSheet, use-tab-actions, tabs.js, tests.

[8] SEARCH RESULT FILTER CHIPS
    Results are grouped by collection with counts already; add tappable
    chips (All · Scriptures · Volumes · WTLB · Studies) ABOVE the results
    that filter the rendered set (the scope engine option already exists —
    this is UI over r.results). Plus a sort toggle (relevance | book
    order) for verse results.
    Effort: S-M.  Files: SearchScreen, SrchGroup, CSS.

[9] "SEARCH IN THIS BOOK/LETTER" ENTRY POINT
    From a Bible book index or letter: a search affordance that opens
    SearchScreen pre-scoped (the engine's `scope` option + searchScope
    plumbing already exist — use-search supports it). Discoverability win.
    Effort: S-M.

[10] AMOLED TRUE-BLACK THEME VARIANT
    A third theme value ("Dark · True Black") that only overrides the
    surface CSS vars (--bg/--bg2/--bg3 → #000-#0a0a0a family). Pure CSS
    vars + the theme cycle button/setting. Verify both annotation colors
    and gold contrast (the WCAG audit method is in CLAUDE.md).
    Effort: S-M.  Watch: theme persistence + boot pre-mount application.

[11] NOTEBOOK COLOR TAGS
    Notebooks are uniform cards; a small color dot (picker in the drilled
    header, reuse HL_COLORS swatch UI) carried on the notebook record and
    shown on cards + note-row chips. Migration-safe: absent color = gold.
    Effort: S-M.  Files: notebook-store (field), NotesIndexScreen, NoteRow.

[12] HOLY DAYS YEAR VIEW (curated data only — no authoring)
    The 16 Holy Days entries as a simple vertical year timeline (their
    existing order/labels), each row tapping through to the entry. Zero
    new content; purely a navigation view over HOLY_DAYS.
    Effort: S-M.

───────────────────────────────────────────────────────────────────────────
TIER 3 — ENGINEERING HARDENING
───────────────────────────────────────────────────────────────────────────

[13] MODAL FOCUS TRAPS (the code half of "deep a11y")
    Sheets/dialogs don't trap Tab focus (desktop PWA + TalkBack nav). One
    small useFocusTrap hook applied to NoteSheet, the pickers, the wipe
    dialog, TabActionSheet, ConfirmStrip-bearing dialogs. Escape handling
    already exists via modalRegistry — wire the trap into the same mounts.
    The TalkBack DEVICE walk stays owner-side, but this unblocks it.
    Effort: M.  Tests: jsdom Tab-cycle tests per sheet.

[14] APP.JSX TARGETED TYPING (CQ5's honest core)
    checkJs on app.jsx was adjudicated too noisy wholesale; the tractable
    version: JSDoc-type the ~15 useState slots + the deps object passed to
    buildScreenRoutes (one @typedef in screen-routes consumed by app.jsx).
    Acceptance: tsc clean, zero `any` regressions in the route contract,
    canary still ≤800.
    Effort: M.

[15] BACKUP VERIFY ACTION (read-only .votbak inspector)
    Settings → Your Data → "Verify a backup file": pick a .votbak, run
    readContainer + validateImportEnvelope + count records/media WITHOUT
    applying, report "Backup from <date>: N notes, N journal entries,
    N media (X MB) — looks intact." Catches a corrupt only-backup BEFORE
    the owner needs it. Reuses the entire import read path minus apply.
    Effort: S-M.  Files: SettingsScreen (+ the same flow on Android needs
    the native begin/read/close bridge calls — web-only first is fine).

[16] JOURNAL EDITOR SESSION UNDO (scoped, not a full undo stack)
    After a destructive block action (delete confirmed / voice memo
    removed), offer the same 6s Undo toast the tab-close uses (snapshot
    the removed block + index; restore splices it back). NOT a keystroke
    undo — block-level only, matching the app's recoverability pattern.
    Effort: M.  Files: JournalEditorScreen + use-tab-actions' toast idiom.

[17] STICKY-NAV/CHROME AUDIT AT 200% BROWSER ZOOM (desktop PWA)
    The 1.5× font-scale walk passed (2026-07-02), but desktop browser zoom
    stacks ON TOP of font scale. One session: 200% zoom walk of the 8 core
    screens, fix any fixed-height chrome clipping.
    Effort: S.

───────────────────────────────────────────────────────────────────────────
TIER 4 — DOCS / META (cheap, compounding)
───────────────────────────────────────────────────────────────────────────

[18] ARCHITECTURE.md REFRESH
    It predates: the real-inert pager peek, MiniSearch-only search, the
    v3 backup container, the modal registry's full reach, bundle-e. One
    session of read-verify-rewrite makes every FUTURE session (any model)
    cheaper. Delete stale sections aggressively ([[doc-pruning]]).
    Effort: M.

[19] GOLDEN SEARCH SUITE EXPANSION
    src/search/golden.test.js pins quality queries. Add the owner's real
    vocabulary: 10-15 queries he actually uses (ask him for his last
    searches), pin expected top-3s. Cheap insurance against ranking
    regressions now that MiniSearch is the only engine.
    Effort: S.

[20] DEVICE-WALK DAY (owner + any model together)
    tools/n1-smoke-walk.md items + the newly shipped confirm flows:
    recording discard confirm, tab bulk-close confirms, annotation hint
    (fresh profile), backup reminder once [4] lands, NTV1/NTV3 confirms.
    Consolidate results back into the checklist.
    Effort: owner time, not model time — schedule it while Fable can fix
    what the walk finds SAME-DAY.

───────────────────────────────────────────────────────────────────────────
SUGGESTED SEQUENCE FOR ~5 DAYS
───────────────────────────────────────────────────────────────────────────
  Day 1:  [1] notes search + [5] smoke assertion  (small, sharp) — then
          [4] backup reminder.
  Day 2:  [3] progress dashboard  (the owner-visible crown piece).
  Day 3:  [6] journal block reorder  (the hardest gesture work — do it
          while Fable is available, it's exactly the kind of subtle
          imperative-DOM task weaker models fumble).
  Day 4:  [2] notes export + [15] backup verify  (data-safety day).
  Day 5:  [13] focus traps + [18] ARCHITECTURE refresh + [20] device-walk
          day with same-day fixes.
  Slot [7]/[8]/[10] into any day that finishes early.

DONE LOG
  (add one-liners here as items land)
  2026-07-03  [1] Notes full-text search — filterNotesByQuery over body+excerpt+source, Bookmarks-parity input on All Notes + drilled notebooks (+9 tests).
  2026-07-03  [5] smoke.js Search step types "shepherd", asserts >=1 result card + >=1 <mark> (RED-proven fail-fast).
  2026-07-03  [2] Share-as-text export — utils/notes-export.js composer (markdown, grouped by source) via navigator.share / PlatformBridge.saveToFile; notebook header + All Notes (+19 tests).
  2026-07-03  [4] Backup freshness reminder — lastExportAt stamped in the shared export success tail; boot toast >30d + >50KB with Settings deep link, 7d cooldown, settings.backupReminder toggle (+18 tests).
              RETIRED 2026-08-04 (owner call): the whole feature — hook, toggle, stamp, CSS — was deleted. Do not rebuild it.
  2026-07-03  [3] My Progress dashboard — hero read/streak/entries, per-collection bars (mark-as-read table extracted to utils/progress-stats.js, shared with Settings), library counts, most-annotated top-5 (Hidden Manna filtered); Library tile via growth-tolerant LibraryOrderStore (+32 tests).
  2026-07-03  [6] Journal block drag-to-reorder — left-gutter grip per block (instant grab, touch-action:none), variable-height FLIP, edge autoscroll, JournalHelpers.moveBlock commit; hardened lifecycle shared with the tabs-drag fix (+7 tests).
  2026-07-03  (unplanned, owner-reported) tab/home/library drag lock-up fixed — see CLAUDE.md 2026-07-03 entry (swallowed grab in the 240ms snap window, any-finger early commit, poisonable refs).
  2026-07-28  [15] Backup Verify — Settings → Your Data → "Verify a Backup": full read-path check (magic/frames/CRC via readContainer on web, the v3Import* drain + native CRC on Android) without applying; always-visible result row; utils/backup-verify.js summarize+format (+9 unit, +3 harness e2e with a real in-test .votbak).
  2026-07-28  [13] Focus traps — hooks/use-focus-trap.js (Tab containment, topmost-trap stack, focus restore, respects dialog autoFocus) on NoteSheet/NotebookPicker/RecordingSheet/TabActionSheet/wipe/import-confirm/garden/disable-tabs (+10 tests). TalkBack DEVICE walk still owner-side.
  2026-07-28  [8] Search filter chips (Scriptures·Volumes·WTLB·Studies + counts, client-side over fetched results) + relevance|book-order sort for verse groups; pure halves in utils/search.js (+9 tests).
  2026-07-28  [9] Scope chip from INDEX screens — goSearch context now covers bible-idx/matthew-idx/volume indexes via COL_BY_INDEX_SC (+3 tests).
  2026-07-28  [16] Journal session undo — 6s Undo toast on confirmed block deletes (text/image/voice); media cleanup deferred past the window + re-checked at fire time (+4 tests).
  2026-07-28  [7] Tab rename + pin — TabActionSheet inline rename (customTitle overrides everywhere incl. document.title); pin moves to pinned prefix + survives Close-others/Close-right/Clear-all; gold pin badge (+7 tests).
  2026-07-28  [11] Notebook color tags — NotebookStore.setColor, drilled-header swatch row (HL_COLORS + gold default), card dot + tinted note-row chips (+3 tests).
  2026-07-28  [12] Holy Days Year view — shipped, then REMOVED 2026-07-29 on owner review ("take away year view" — didn't earn its tap). List view was never touched. Do not re-add.
  2026-07-28  [10] True Black (OLED) — dark-theme MODIFIER (settings.trueBlack + body.amoled; --bg2/--bg3 only) instead of a third theme value: nav cycle + thumbnail slots untouched; boot pre-paint applies it (+2 tests). → RETIRED 2026-08-03 on owner review ("just make it default, looks better anyway"): the pure-black surfaces ARE the dark theme tokens now; toggle, body.amoled, boot clause all deleted (tombstone tests). Do not re-add a toggle.
  2026-07-28  [14] app.jsx joined tsc (clean) + the 124-prop ScreenRouteDeps typed-assignment route contract (RED-proven: renamed prop = TS2561). Canary 800/800.
  2026-07-28  [18] ARCHITECTURE.md — FlexSearch mentions retired + a Current-systems addendum (MiniSearch, 8 bundles, v3 container, modal registry/focus traps, pager peeks, thumbnails, frame pacing, themes).
  2026-07-30  [17] 200% zoom audit — DONE, PASSES. Unblocked by a session with the Browser pane. Done GEOMETRICALLY rather than visually (stronger for this question): 200% zoom halves the CSS layout viewport while px-pinned chrome keeps its size, so emulated as 640x400 (from 1280x800) STACKED with --font-scale:1.6, the documented worst case. DOM probe measured every visible element for horizontal overflow + content clipped by a non-scrolling fixed height across Home / Volumes / Volume One / Letter / History / Settings / Library / Scriptures / Tabs overview: clipped:[] and scrollWidth===640 everywhere. The one tall chrome piece, .sel-toolbar (no max-height, 157px after the ▲/▼ retirement), raised on real reading text: top 77 / bottom 234 in a 400px viewport = fully visible. hOverflow hits are all .pager-peek subtrees, parked ±one viewport BY DESIGN. CAVEAT: screenshots unavailable (pane must be displayed to composite), so this is layout geometry, not pixel polish — a visual pass is still worth doing if someone has the pane up.
  STILL OPEN (both need the owner, not model time): [19] golden-suite expansion — needs his real search vocabulary (ask him for 10-15 queries he actually uses, pin expected top-3s); [20] device-walk day — his time, tools/n1-smoke-walk.md.
  ── ORIGINAL [1]-[20] OTHERWISE CLEARED: 18 of 20 landed; measurement-track entries continue below. ──
  2026-08-03  [26] Smart-resume chips — index rows/cards show "N% · ~M min left" for in-progress items (frontier via progressKeyFor → ReadingStatsStore.getProgress); cold "~N min" otherwise (+2 tests). History rows skipped (different key namespace — do on ask).
  2026-08-03  Companion rail (Sol #4) — fn/scripture sheets dock as a NON-modal side panel in the right gutter at >=1640px; complementary role, no trap/backdrop/aria-modal; bottom sheets unchanged below; inline LetterView/WtlbEntryView sheets included. Live-verified at 2048px.
  2026-08-03  Brainstorm reconciliation — words-based collection bars (groupWordStats, idle) + lifetime Reading Time stat; strays filed as [27]-[30].
  2026-08-03  [25] Translation facts — measured length deltas appended to 7 picker descs ("~3% longer than NKJV"; NKJV 770,492 words full canon, probe 2026-08-03). Hardcoded by design: corpus files are frozen, numbers can't drift.
  2026-08-04  [27] Autoscroll wpm-first readout + [28] density-ranked Most Annotated (marks/1k words) — both RED-proven; see the item bodies below.

MEASUREMENT TRACK (added 2026-08-03 — post-backlog; the reading-measurement
engine landed that day: word counts, multi-vector read detector, ReadingStats
ledger, frontier resume, wpm, My Progress surfaces. [25] is DONE and [26] is
PARTIAL; the rest are deferred follow-ons, each one-session-scoped.)

[21] PER-LETTER SKIM INDICATOR — ★ OWNER EXPLICITLY WANTS THIS, HOLD UNTIL
     HE ASKS ★ (2026-08-03: "the subtle per-letter skim indicator is also
     smart, but hold it for me and don't let me forget it in the future").
     The data ALREADY ACCUMULATES: ReadingStatsStore.progress[key].c is the
     per-segment credited set. Build: a subtle indicator on a read letter
     whose middle segments were never credited ("you skimmed the middle"),
     probably in the index row or letter header. Design taste matters —
     subtle, not shaming. Depends on nothing further.
[22] TTS (text-to-speech) with word highlighting — OPEN, BUT RE-SCOPED
     2026-08-09. The headline half of this item SHIPPED, and better than
     specified: read-along already paints the sentence being spoken, in a
     REAL HUMAN VOICE rather than a synthetic one (ReadAlongHighlight.jsx +
     data/audio-sync.js, timings forced-aligned offline by
     tools/align-audio.py). It paints through the CSS Custom Highlight API
     over a Range — no DOM mutation at all — which is strictly better than
     the "reuse the highlight painter" plan sketched below: annotations,
     live selection, and the read detector are untouched by it.

     So do NOT build this as written. What is actually left is the FALLBACK
     leg, and it is worth having: alignment data exists for 230 of the 729
     tracks in AUDIO_MANIFEST (measured 2026-08-09), so ~499 letters and
     entries play with no read-along at all, and letters with no recording
     have neither voice nor highlight. Remaining value = TTS as the
     degraded path where forced alignment is absent — SpeechSynthesisUtterance
     boundary events give charIndex into the flattened textContent, which
     lands in the same offset domain audio-sync.js already uses, so it can
     drive the EXISTING ReadAlongHighlight painter instead of a second one.

     Decide before building: whether a synthetic voice is wanted at all on
     a corpus whose recordings are part of its character (owner call), and
     whether TTS playback should count as read-tracker dwell the way real
     playback does. Cheaper than the original item now that the painter,
     the offset domain, and the player plumbing all exist.
[23] READING MILESTONES — DONE 2026-08-04 (milestones half). Ten
     READING_MILESTONE_DEFS (items/words/rereads) unlock inside
     recordCompletion, which now RETURNS the newly-unlocked defs so
     useMarkAsRead can toast via the existing jrnShowMilestoneToast;
     My Progress gained a Milestones section (locked rows shown dim).
     REMAINING: the full month streak calendar — the 14-day strip still
     ships. Original spec follows.
     READING MILESTONES + words/day streak-calendar depth — JournalStats'
     milestone pattern applied to ReadingStatsStore (100k/500k/1M words,
     "read Psalm 119", full-volume completions); upgrade the streak display
     with wordsForDays() bar depth (a 14-day strip shipped 2026-08-03;
     this is the full calendar). Toast via jrnShowMilestoneToast pattern.
[24] YEAR IN REVIEW screen — composed entirely from ReadingStatsStore +
     JournalStats + ReadingStreak data that already accrues. Seasonal;
     build near year-end.
[25] TRANSLATION FACTS — DONE 2026-08-03. Seven non-NKJV picker descriptions
     now state the measured whole-canon length delta. Hardcoded by design:
     the shipped translation corpora are frozen, so the facts cannot drift.
[26] SMART RESUME EVERYWHERE — DONE 2026-08-10 (C2-C [C6]). Index rows/cards
     shipped the "N% · ~M min left" chip 2026-08-03; History chapter rows
     joined 2026-08-09; History LETTER rows — the named remainder, held back
     only by the different key namespace — landed 2026-08-10. The mapping is
     COL_BY_INDEX_SC.get(row.volumeScreen).readKey, which is both the id
     bookItemsFor resolves the collection under and the middle segment of the
     tracker's v1:<source>:<item> key, so a History row, its index card and
     the frontier record cannot disagree. Study-chapter rows stay chipless
     (their index has no chip to match). The original "reading dot" target
     was corrected by C2 recon 2026-08-03 (dot already resumes).

[27] AUTOSCROLL SPEED IN WPM — DONE 2026-08-04. The pill readout leads
     with the measured wpm ("~120 wpm · 30 lines/min") once the page is
     measured, so ± tunes to a wpm target; lines/min stays the stored
     unit (survives text resizing). Settings row stays lines/min — no
     page there to measure, and the honest number needs the page.
[28] ANNOTATION DENSITY /1k WORDS — DONE 2026-08-04. mostAnnotatedSources
     ranks by marks-per-1k-words (countItemWords via bookItemsFor /
     findEntryContext.entry); unresolved-corpus sources fall back to raw
     count. My Progress tally shows "N marks · D/1k words".
[29] DENSITY SURFACES — (b) DROPPED 2026-08-04, with reason: 31,102 of
     31,751 index docs are single-verse docs where "depth" is always 1,
     group-level counts already ship (SrchGroup), and positional depth is
     unmappable because index-builder flattens a letter to one string with
     no block offsets. Investigating (b) DID surface a live P1 — the
     Matthew Study Bible contributed ZERO search documents (fixed; see
     HISTORY 08-04 s6) — which was worth more than both halves.
     (a) Matthew commentary-weight badge DONE 2026-08-10 (C2-C [C7]) —
     `utils/matthew-note-weight.js` holds the per-chapter table, frozen and
     measured (it reproduces every figure below); ChapterIndex paints
     "N.Nx notes" under the title on matthew-idx only. countItemWords was
     NOT touched — the badge is the separate signal the contract demands.
     Measured for real:
     verse text 23,221 words / 1,071 verses vs votNotes 49,752 words / 627
     notes = 2.14x commentary, ranging 0.00x (ch 2) to 8.43x (ch 24). Note
     the contract: study panels are supplemental and excluded from
     countItemWords by design (same call as the prophecy cards), so a badge
     must be a SEPARATE signal, not a change to the ~N min chip.
[30] STORAGE GROWTH SPARKLINE — DONE 2026-08-04. Settings → Your Data →
     "Growth": a sentence (the actual feature) + aria-hidden div bars.
     Samples ride the already-registered `meta` IDB store — no new
     CachedStore, no DB_VERSION bump, and `meta` is excluded from
     USER_DATA_STORES so the series can never inflate the number it
     trends. One sample per Settings mount (where measureUserData already
     runs), same-day overwrite, capped at 60.
═══════════════════════════════════════════════════════════════════════════

================================================================================
CAMPAIGN 2 QUEUE (written 2026-08-10, post-s9 triple recon — audio residuals /
reading-flow / technical). Full evidence with file:line cites lives in the three
recon reports; this is the adjudicated working order. Strike items as they land.

C2-A  LISTENING TRUTH + CONTINUATION (the big one) — ALL DONE 2026-08-10
      (de6665c = A1/A2/A3/A5 · 0c9dac7 = A4/A6/A7/A8/A9 + the switch warning;
       the two owner directives landed first in 2647e70.)
  [A1] DONE 2026-08-10 (de6665c). playTrack locates the recording in the LIVE
       manifests by immutable URL and rebuilds around it — a Bible chapter gets
       its book from that chapter forward, a letter its collection forward on
       the named rendition/part. Only a URL no manifest carries still plays alone.
  [A2] DONE 2026-08-10 (de6665c). recordPlayed left _start() for _countPlay()'s
       four user-initiated entry points; the two counters are isolated so a
       failing shelf write cannot cost the play count.
  [A3] DONE 2026-08-10 (de6665c). Bible tracks notify PER TRACK regardless of
       queue shape, carrying the chapter; __votAudioListened resolves bible-*
       into the v1:<bookId>:<chapter> key space BibleChapterView already writes.
  [A4] DONE 2026-08-10 (0c9dac7). _tracksFor titles per-chapter shapes by
       chapter ("Psalms 117"); partLabel still carries "Chapter N" for the desk
       head, the jump-to-text and the read credit.
  [A5] DONE 2026-08-10 (de6665c). ChapterView's hero gained the pill, fed by
       bibleAudio threaded through MatthewChapterView from the route's prop.
  [A6] DONE 2026-08-10 (0c9dac7). 30s floor governs the WRITE path; a new queue
       replacing the old is a boundary write; the desk says "Resuming…" instead
       of describing a boot placeholder; the window follows auto-advance and the
       expanders announce through a live region mounted empty.
  [A7] DONE 2026-08-10 (0c9dac7). Sleep chips show the armed preset; Voice joins
       Speed as radiogroup/radio/aria-checked; the media card's second line names
       the EDITION for a Bible chapter.
  [A8] DONE 2026-08-10 (0c9dac7). Bible rows gained the Text affordance the
       blanket !bible gate withheld, plus a per-book chapter disclosure whose
       taps start the book at that chapter.
  [A9] DONE 2026-08-10 (0c9dac7). 'Heard to the End' (1/10/50/250/1000) reads
       the completions counter, which had reached no achievement since it shipped.
  [WARNING] DONE 2026-08-10 (0c9dac7, owner directive). A voice switch rebuilds
       the queue from the corpus, so the two queues that cannot survive it ask
       first through ConfirmStrip: one the listener reordered or pruned, and a
       lone recording about to become a whole collection. Plain queues switch
       on the first tap, unchanged.

C2-B  TRUTH-AND-COPY PASS (one commit)
  [B1] About page 2 claims nothing is downloaded except Garden images — false
       since streaming shipped; also never mentions the Listening Library. S
  [B2] "Whole-book audiobook" copy on Settings/AudioLibrary/comments — all
       three editions are per-chapter. S
  [B3] use-autoscroll lease block says FOUR writers; five are sanctioned and
       CLAUDE.md points every future session at that block. S
  [B4] CLAUDE.md drift: latest-landed re-head, 54->59 screens, per-bundle sizes
       (aggregate right, parts stale), file-structure counts (or delete per CQ3). S
  [B5] ARCHITECTURE.md: re-date; add the AUDIO SECTION — a ~4,200-line
       subsystem has zero deep reference (trust boundary, shape-branching,
       seek contract, forward-only horizon, positions store, native card). L
Owner-gated: two Milestones engines (10-row strip vs 84-item screen) need a
       canonical-table decision; backup "Last export" line stays retired.

C2-C  READING-PATH COHERENCE — ALL DONE 2026-08-10
      ([C1]-[C7] in one commit, [C8]-[C10] in a second.)
  [C1] DONE. The sheet registers 'wtlb-scripture-sheet' with useModalRegistry
       (gated on scriptureRef), so use-autoscroll's isModalOpen() finally sees
       it. Registration only — Escape stays single-source in useAndroidBack.
       __closeSheet wiring untouched. Live-verified on a Part One entry.
  [C2] DONE. LetterView's three "Volume Two" fallbacks → 'Library' / an
       omitted eyebrow half / a NULL collectionLabel (sub is nullable through
       the whole Track shape); the ONE route that rode the fallback
       ('vot-letter') now names Volume Two itself. HistoryEntryCard resolves
       through the registry, honours the legacy numeric `volume`, and drops
       the line entirely rather than guessing. ChapterView's five "Matthew"
       literals → one derived bookLabel; a title-less book drops the book half.
  [C3] DONE. backLabel: partLabel || 'Index' — "Back to Part One" /
       "Part Two" / "The Blessed" / "Regarding The Holy Days". smoke-ci's
       compact-nav assertion re-pointed at the real destination.
  [C4] DONE. The body class is WATCHED (MutationObserver on <body>'s class)
       instead of polled; the ResizeObserver + 500ms re-attach interval now
       exist only while the notch is on. Default-off readers paid a 2 Hz
       timer on every screen for the life of the app.
  [C5] DONE. Presence guard (videos[]/videoVoiceUrl/videoMusicUrl) matching
       its four siblings. Real blast radius, measured live: all 348 numbered
       letters DO carry video, so the empty card was showing on 69 of 72
       Bible-study chapters (LetterView hosts them) + Hidden Manna.
  [C6] DONE — CLOSES [26]. Letter rows join chapter rows: one key space,
       bookItemsFor keyed by the collection readKey COL_BY_INDEX_SC resolves
       from the row's volumeScreen, tracker key v1:<source>:<item>. Lazy per
       rendered row, one corpus sweep per source, none for a collapsed group.
  [C7] DONE — CLOSES [29a]. utils/matthew-note-weight.js ships the measured
       per-chapter table (frozen; reproduces [29a]'s 2.14x / 0.00x ch2 /
       8.43x ch24); ChapterIndex renders "8.4x notes" UNDER the title on the
       study index only. Under, not beside: measured live at 375px, a second
       right-hand pill crushed the title column 145px -> 48px. countItemWords
       untouched, per the [29a] contract.
  [C8] DONE 2026-08-10. SearchScreen's input gains aria-label "Search" and
       its clear button "Clear search" (it announced as the glyph); all FOUR
       top-nav cluster icons carry explicit aria-labels beside their titles
       (the titles are load-bearing CSS selectors and stayed byte-identical —
       ThemeBtn joined the three named here, same defect, same cluster);
       History's four accordion levels announce aria-expanded; the Surprise
       FAB's tooltip and label became ONE string — the descriptive one, but
       opening with the visible caption so label-in-name (2.5.3) still holds,
       which a bare swap to the old title would have broken.
  [C9] DONE 2026-08-10. The input is type="search" + `notes-index-search
       hlx-search` + aria-label; the three .hlx-search rules moved to app.css
       right after .notes-index-search (equal specificity, later, so they
       still win every property they declare). Two neutralisers added — the
       shared 0.85rem bottom margin would double .hlx-controls' flex gap, and
       the shared focus BACKGROUND would repaint a box whose own focus rule
       only ever moved the border. Verified byte-identical by a live
       computed-style diff at rest AND focused (16 properties, zero diff).
       Nothing else moved: no other .hlx-* rule references .hlx-search.
       FOUND EN ROUTE: `--border` is referenced 8x (app.css x4 + the injected
       sheet x4) and DECLARED NOWHERE, so those borders have never painted —
       the phantom-token class the type-scale gate catches for --fs-* only.
       Left alone here (fixing it changes the look); tracked separately.
  [C10] DONE 2026-08-10 — WIRED, not deleted. 'source-az'/'label-az'
       (Bookmarks) and 'source-az'/'target-az' (Links) were implemented in
       each comparator and unreachable from a two-state toggle. The same one
       button now CYCLES all four (the shape NotesIndexScreen/LinksScreen
       already use), title "Cycle sort order", modes declared in one
       SORT_CYCLE table per screen so the affordance and the comparator
       cannot drift apart again. First tests for BookmarksScreen and
       HighlightsScreen while here (BACKLOG [D6] names both gaps).

C2-D  SAFETY NETS
  [D1] DONE 2026-08-10. NOT ratcheted — deliberately. Measured is 86.68 /
       77.17 / 87.48 / 91.82 (statements/branches/functions/lines, 3,941
       tests); a floor a point under that fails the next honest commit that
       adds an uncovered branch, so the four aggregates stay ~26 points down
       as a COLLAPSE detector and the tight ratcheted numbers stay the
       per-file floors. What was actually broken: pre-commit ran the
       coverage-free `npm run test`, so the floors were a post-push CI check
       only. It runs `npm run test:coverage` now (66s vs 47s, measured);
       `npm run test` stays coverage-free for the watch loop.
  [D2] DONE 2026-08-10. The parity test gains its FOURTH leg —
       IDBAdapter.STORE_NAMES — which is the only one that can see a store
       all the others forgot together: it starts from the schema, not from
       the other lists. RED-proven (with the three reverted it names all
       three by name). vot-library-order / vot-note-default join the export
       map + STORE_SHAPES + USER_DATA_STORES; vot-ann-hint-dismissed joins
       _flagStores() as the fourth flag. Import already tolerated their
       absence (`if (!(name in storesObj)) continue`), so old .votbak files
       restore unchanged. NoteDefaultStore gained a replaceAll that routes
       through set() — set takes two positional args, and normalizing on the
       way in means a hand-edited backup cannot leave the store in a state
       its own UI cannot produce. Leg 4 also pins `meta` as the ONE exempt
       schema store, by exact match.
  [D4] DONE 2026-08-10 — MERGED, not documented-as-lossy, because the two
       halves of vot-state want opposite policies. Session fields (tabs,
       activeTabIdx, theme, settings, activeReadKey) STAY last-writer-wins:
       a stomped write costs nothing durable (the losing tab still holds its
       copy and rewrites on the next tick) and merging two tab strips would
       invent a layout neither window had. The accumulating maps merge:
       readItems is the marked-as-read ledger behind My Progress, the streak
       and 84 achievements, and a mark made in tab A and clobbered by tab B
       was permanently gone. All three maps go through mergeMapByKey WITH
       base, so unmarkRead / clearReadForBook / clearAllProgress are honored
       instead of resurrected; counts resolve to the higher of the two.
       Found en route: `_save` returned early for merge stores and the LS
       boot shim lived AFTER that return, on a comment that read "no merge
       store uses lsShim" — vot-state is now both, so the shim hoisted above
       the split and stays a synchronous write. RED-proven by 9 tests.
  [D3] DONE 2026-08-10. tools/check-bundle-budget.js — nine per-file byte
       ceilings ~15% over the size measured at landing, in pre-commit
       (after the rebuild) + CI. Names the grown bundle, prints its growth
       vs baseline, and says re-baselining means editing the number in the
       tool in the same commit. No skip flag, on purpose.
  [D5] DONE 2026-08-10. JaCoCo 2 -> 5 classes, and the blended bundle floor
       became five per-class floors (BoundedLogTree .95 / MainActivityLogic
       .95 / JsBridge .65 / AppInterface .52 / GardenImageCache .48, each
       under measured). The other five tested classes stay out because
       Robolectric's sandbox classloader bypasses JaCoCo's agent and they
       report 0-0.4% while fully tested — a 0% floor gates nothing.
       U17's "GardenImageCache drags the aggregate" finding is obsolete:
       per-class is exactly the shape that objection asks for.
  [D6] Tests: BookmarksScreen (7 destructive paths), HighlightsScreen,
       ChapterView, AudioSeekSlider, ReadingMinChip; nav-index.js 37%. M
  [D7] CLOSED 2026-08-10 — NOT WIRED, on measurement. The premise was
       wrong twice over: bible-studies.js rides no bundle at all (it is
       raw-injected by src/data/translations.js:85), and the win is not
       1.2 MB. Minified it goes 4,380,459 -> 3,164,053 bytes (-27.8%), but
       every path that serves it compresses: gzip -9 is 969,492 raw vs
       901,233 minified, so the real saving is 68,259 bytes (-7.0%) on a
       one-time, SW-cached lazy fetch — and the same 7% in the APK, whose
       assets are deflate-stored. Wiring it costs five integration points
       (build.py output, the translations.js loader path, the SW cache
       BUCKET it moves between, gradle ignoreAssetsPatterns, and
       check-apk-assets) plus a 3.2 MB generated blob committed beside the
       4.4 MB source. Not worth 68 KB. Re-open only if the nine alternate
       translations (~4.5 MB each, same shape, same loader) ever justify
       the pipeline for the whole family at once.
================================================================================
