═══════════════════════════════════════════════════════════════════════════════
VOTReader-studio — Strategic Working Memory
═══════════════════════════════════════════════════════════════════════════════

Started: 2026-05-10
Last pruned: 2026-05-24 (P7a landed; Q3 closed; May-10 research notes archived to HISTORY.md)
Status: ACTIVE — kept slim as work progresses
Companions: CLAUDE.md (live status), HISTORY.md (landed work), ARCHITECTURE.md (system ref)

───────────────────────────────────────────────────────────────────────────────
PURPOSE
───────────────────────────────────────────────────────────────────────────────

PLAN.txt is the strategic compass: what's next, what's deferred, and why.
It is NOT the live status doc (that's CLAUDE.md's "Current state" block) nor
the work log (that's HISTORY.md). Keep it small and high-signal — when an
item lands, move the prose to HISTORY.md and trim the entry here to a one-
liner referencing the commit.

The plan is intentionally phased so:
- Each phase is independently shippable
- Earlier phases reduce risk for later phases
- We never break working features mid-refactor

For the historical May-10 research-agent reports + the Phase 0-5 detailed
sprawl that produced JSX conversion + bundles + hooks extraction, see
HISTORY.md (specifically the Q2.7 + P6 + Objective G sections).

───────────────────────────────────────────────────────────────────────────────
PHASE STATUS — quality-uplift sequence (Q3 → Q4 → Q5 → Q6)
───────────────────────────────────────────────────────────────────────────────

  Q1 (CI day-one)             ✅ DONE  — `.github/workflows/ci.yml` build + syntax-check
  Q2 (modularization)         ✅ DONE  — 130+ ES modules, 4 cluster bundles, P6 hooks
  Q2.7 (JSX conversion)       ✅ DONE  — every component is JSX
  Q3 (ESLint)                 ✅ DONE  — 0 errors / 0 warnings, CI `--max-warnings 0`
                                          locked, lint-staged in pre-commit
  Q4 (JSDoc / tsc --checkJs)  ✅ DONE  — 37 files typed, CI gate zero-tolerance
  smoke-lite.js for CI        ✅ DONE  — Node CLI; globals + collections + cycles
  Q5 (Vitest)                 ✅ DONE  — 218 tests / 9 files; ratchet 1→14 stmts
  App() decomposition (P7)    🚧 ACTIVE — P7a + P7b landed; 275 tests;
                                          App() 2,261 → 2,254 lines
  Q6 (CSS hardening)          ⬜ PENDING

───────────────────────────────────────────────────────────────────────────────
POST-Q4 SEQUENCING (executed 2026-05-24)
───────────────────────────────────────────────────────────────────────────────

The pinned order (1) smoke-lite → (2) Q5 Vitest → (3) App() decomposition was
executed in that order. Rationale: high-risk structural refactors come AFTER
test coverage on the affected surface (memory [[refactor-after-tests]]).

  1. smoke-lite.js ✅  Node-runnable safety net for CI; vitest imports
                       runSmokeChecks for module-graph + globals assertions
  2. Q5 Vitest     ✅  218 tests / 9 files; coverage ratchet locked at the
                       Q5 baselines, bumped to 14/11/12/15 floors at Q5.9
  3. App() decomp  🚧  P7a landed; see backlog item 3 below

───────────────────────────────────────────────────────────────────────────────
POST-Q4 BACKLOG (priority order — top = next)
───────────────────────────────────────────────────────────────────────────────

1. APP() DECOMPOSITION (P7 — ACTIVE)
   ─────────────────────────────────
   App() = 2,256 lines after P7a (was 2,191 at P6 close; grew to 2,261
   during Q4/Q5 from JSDoc + small changes; 2,261 → 2,256 after the first
   extraction). Target: under 800 lines.

   ── Structural measurement (taken 2026-05-24 post-P7a) ────────────────
   App() body: lines 24–2253 (2,230 lines total inside the function).
     - Logic block:     lines   24–1151  →  1,128 lines  (hooks + callbacks
                                              + effects + nav-helper glue)
     - JSX render tree: lines 1152–2253  →  1,103 lines  (55 conditional
                                              `{screen === "X" && ...}` blocks)

   The 50/50 split means hook + callback extraction CANNOT hit <800 alone.
   Maximum-extraction-of-logic floor ≈ 1,103 (the render tree by itself).
   Render-tree decomposition is required, in a separate phase with a
   distinct risk profile.

   ── Phase 1: logic extraction (active; P7a + P7b landed) ────────────
   Goal: ZERO inline concerns left in App()'s logic block. Everything
   that remains is hook composition + render-tree-adjacent wiring.

   ## METRIC: concerns extracted, NOT lines removed.
   Memory [[concerns-not-lines]]: hook extraction relocates code, it
   doesn't delete it. Call-site wiring (import + destructure + PARAMS
   object + breadcrumb) typically adds back 10-30 lines per extraction,
   nearly offsetting the body that moved out. P7a + P7b moved ~330
   lines of logic into hooks and only dropped 7 lines from App() —
   that's the expected arithmetic, NOT a failure. Phase 1's exit
   criterion is the concerns checklist hitting zero. Line count at
   that point is whatever the wiring takes (~1,400-1,600 estimate
   stands as illustrative, not a target).

   Workflow per extraction: P6 pattern + Q5 vitest validation.
     1. Identify OWNS/PARAMS/RETURNS boundary
     2. Extract to src/hooks/, JSDoc-type it, write vitest coverage
     3. Wire into _entry-b.js, replace call site in App() with breadcrumb
     4. All 5 pre-commit gates + smoke verify
     5. Update CLAUDE.md "Current state" line count

   Landed:
     P7a — useNavHistoryTracking (2026-05-24): auto-track-history effect.
           Single 18-line useEffect moved to its own 115-line hook with
           23 vitest tests. Coverage ratchet held + bumped: branches
           11→13. Net App() shrink: 5 lines (architectural win is bigger
           — the most debt-laden disable cite moved out).

     P7b — useNav (2026-05-24): 20 simple nav-chrome helpers extracted
           (goHome / goScripturesHome / goScriptureGenre / goVolumesHome
           / 8 navOrigin-pattern: goSettings, goHistory, goAbout,
           goLibrary, goJournalHub, goNotesIndex, goLinksIndex,
           goBookmarksIndex, goHighlightsIndex / 2 journal nav:
           goJournalViewer, goJournalEditor / 5 trivial switchers:
           goColIdx, goMatthewIdx, goStudiesHome, goBibleIdx,
           goToGardenFirst). Pilot principle applied
           ([[low-risk-pilot-first]]): the 4 complex helpers (goTabs /
           goNavOrigin / goSearch / goSearchOrigin) deliberately left
           for P7c — they need refs + thumbnail-helper deps + the
           26-line search-context computation + window.__goSearch
           bridge effect. The 8 navOrigin-pattern setNavOrigin literals
           deduplicate to one private `_captureOrigin()` inside the
           hook. 34 new vitest tests. Coverage ratchet bumped:
           stmts 14→17, fns 12→16, lines 15→17 (branches held at 13).
           Net App() shrink: 2 lines (architectural win again bigger).

   ## CONCERNS REMAINING IN App() (Phase 1 checklist, 9 left)

   Extracted so far (3): useHistory (P6c) · useNavHistoryTracking (P7a)
                        · useNav (P7b)

   Remaining (rough order, bottom-up by dependency):

     1. useComplexNav (P7c — next).
        goTabs / goNavOrigin / goSearch / goSearchOrigin + the
        window.__goSearch binding effect. The 4 nav-chrome helpers P7b
        deferred. Needs navOriginRef + searchOriginRef +
        flushScrollToActiveTab + captureActiveTabThumbnail +
        settings.tabsEnabled. App.jsx lines ~554-606.

     2. useBibleStudies.
        STUDIES / getStudyById / getStudyChapter / studyReadKey /
        selectStudy / selectStudyChapter / MATTHEW_CHAIN_ENTRY /
        CHAIN_ORDER / UNIFIED_CHAIN / chainIdx / prevChainEntry /
        nextChainEntry / goToChainEntryFirst / goToChainEntryLast.
        Resolves the original TDZ issue from P7a/P7b call sites
        (those have to live below this block today). App.jsx lines
        ~889-995.

     3. useReadingChainNav.
        _first / _last / _firstPreface / _goFirst / _goLast /
        boundaryConfig / bookIdx / prevBibleBook / nextBibleBook /
        goNextBibleBook / goPrevBibleBook / chIsFirst / chIsLast /
        bcv* boundary props / goToRevelationLast. Cross-volume +
        cross-book boundary navigation. Tied to useBibleStudies (uses
        STUDIES) — consider sequencing right after. App.jsx lines
        ~1013-1116.

     4. useMarkAsRead-helpers (rename existing useMarkAsRead → it owns
        only the window bridge today).
        VERSION_ID / getReadKey / isRead / markRead / unmarkRead /
        clearAllProgress. Either fold into the existing useMarkAsRead
        hook (rename to useMarkAsReadHelpers? or keep two hooks). 16
        call sites across the render tree. App.jsx lines ~612-624.

     5. useReadingPositionNav.
        setLastReadForVol / saveProphecyCardStates /
        prophecyCardStatesRef / selectMatthewCh / selectBibleCh /
        goToLastRead. Reading-cursor coordination. goToLastRead has the
        same TDZ issue as P7a (calls getStudyById/selectStudy from
        Bible Studies block) — sequence after useBibleStudies.
        App.jsx lines ~247-256, ~627-651, ~885, ~1007.

     6. useHandlers (or useDispatchers).
        handleScriptureSelect / handleVolumeSelect / handleSearchSelect /
        handleSearchCommand / handleSelect / handleSurprise +
        srchVolLookup / SRCH_VOL_MAP / srchResolveLetterId. App-shell
        dispatch entry points from various pickers/cards. Largest
        single concern (handleSearchSelect alone is ~80 lines).
        App.jsx lines ~653-783, ~849-882.

     7. useTapThrough.
        goToLetterFromMatthew / openInAppLetter. Inbound deep-link
        openers from study/letter content. App.jsx lines ~259-298,
        ~308-351.

     8. useJournalMutations.
        createAndEditJournal (and maybe future journal-mutation
        helpers). Small (~13 lines) but a distinct concern.
        App.jsx lines ~537-551.

     9. useAppShellEffects (cluster of small useState + useEffect
        clusters that don't fit anywhere else):
          - theme + setTheme + theme-application useEffect (line ~147)
          - showWelcome + dismissWelcome + welcome useEffect (line ~394)
          - isOnline + the online useEffect (line ~398)
          - settings-sync useEffects (lines ~362, ~369, ~377)
          - __bumpHlTick window bridge (line ~138)
        Each individually small (5-15 lines); group might fold into a
        few small hooks rather than one big one. Lowest priority.

   STAYS in App() (composition / wiring, not concerns):
     - 12 existing hook CALLS (useSettings, useTabs, useThumbnails,
       useReadingDwell, useFromLetterStack, useNavigateToLink,
       useAndroidBack, useHistory, useNavHistoryTracking, useNav,
       useScrollMemory, useSavedState) — App() is the composer
     - 12 tabField destructures (just tabField call sites)
     - Standalone useState declarations (theme, journalEntryId, etc.)
       wire into hook params; they're AppShell state, not concerns
     - ALL_BOOKS / book / chapter / window.__ALL_BOOKS (1-2 lines each)
     - 16 _findLetter calls + _findLetter itself (data wiring used by
       the render tree)
     - tabsCtxValue React.useMemo (line 1118) — Context provider value
     - colReadNavProps / colIdxProps helpers (lines 1127-1138) —
       render-tree prop-derivation
     - The render tree (Phase 2)

   ──
   Phase 1 done = checklist above hits 0. Track items removed per
   commit; do NOT track App() line count as the success metric.
   Estimated 8-12 more extraction commits to clear the list.

   ── Phase 2: render-tree decomposition (PENDING, after Phase 1) ──────
   Goal: get App() under 800 lines by pulling per-screen JSX into wrapper
   components and collapsing the `{screen === "X" && ...}` dispatch into
   a ScreenRouter.

   Render-tree shape (measured 2026-05-24 post-P7a):
     29 substantive screen blocks: 867 lines total. Top 5 hold ~447:
       240 lines  garden-view              (lines 2013–2253)
        87 lines  bible-study-chapter      (1742–1829, has IIFE)
        42 lines  matthew-ch               (1581–1623, has IIFE)
        40 lines  bible-ch                 (1679–1719)
        37 lines  holy-days-index          (1947–1984)
        31 lines  history                  (1336–1367)
        30 lines  journal-viewer           (1419–1449)
        25 lines  links-index              (1485–1510)
        22 lines  notes-index              (1463–1485)
        21 lines  settings                 (1268–1289)
        20 lines  studies-home             (1623–1643)
     26 trivial letter/index 4-line wrappers: ~104 lines (collapse all
     at once via the ScreenRouter pattern).

   Phase 2 sequencing — low-risk PILOT first, high-risk after:

     P7-2a (FIRST):  ScreenRouter pattern over the 26 trivial 4-line
                     letter/index wrappers. ~104 lines collapsed by a
                     lookup table replacing the switch chain. Near-zero
                     risk: the wrapper div stays identical (each entry
                     was already `{screen === X && <XScreen … />}` — a
                     dispatch table renders the same XScreen with the
                     same props, just chosen by `screens[screen]`). No
                     CSS selectors break, no ErrorBoundary moves, no
                     effect-fire semantics change. The mechanical win
                     proves the ScreenRouter shape works before any
                     substantive screen extraction touches it.

     P7-2b, 2c, …:   Substantive screen extractions in size-descending
                     order: garden-view (240) → bible-study-chapter (87)
                     → matthew-ch (42) → bible-ch (40) → holy-days-index
                     (37). Each is its own commit. The risks below
                     (CSS, useEffect, ErrorBoundary) apply ONLY to
                     these — but they ride on the ScreenRouter pattern
                     proven in 2a.

   Workflow per substantive extraction (P7-2b onward, NOT 2a):
     1. Extract the screen block into ui/screens/XScreen.jsx (most
        screens already have this file shape; this is moving the
        last conditional-render wrapper into the screen file itself).
     2. Update the App() conditional to a single `<XScreen … />` call
        (or, eventually, collapse into ScreenRouter).
     3. Run smoke walk (votSmoke or in-browser nav of the affected
        screen) — NOT just vitest. Render extraction can break CSS
        selectors that target `.app > .screen-container > .X` even
        when component logic is preserved.
     4. Visual diff: before/after screenshot of the screen. Layout
        shifts from a changed component hierarchy are silent to tests.
     5. ErrorBoundary placement: if a screen had an inline error
        wrapper, the extraction must preserve it (or the boundary
        moves with the component — verify on purpose-broken render).

   Render-tree risks (call out so they don't surprise):
     - CSS selectors that traverse the App-level wrapper (`.app > .x`)
       break if the component hierarchy changes depth.
     - `useEffect(..., [screen])` inside child components fires
       differently from the App-level one (the parent's screen change
       unmounts the child rather than re-running its effects).
     - Sheet/portal placement: screens that render sheets via portals
       still work after extraction, but sheets that READ closure values
       from App() need explicit props.

   Realistic Phase 2 reduction: ~700–800 lines (5–7 biggest screens
   extracted + ScreenRouter for the 26 trivial dispatchers + ~100 lines
   of boilerplate eliminated). Lands App() at ~700–900.

   <800 IS achievable, but it requires BOTH phases. Do not promise
   <800 from Phase 1 alone.

2. useSyncExternalStore MIGRATION (eliminates Bin 4 hlTick cites)
   ─────────────────────────────────────────────────────────────
   Q3.3e added 24 disable cites for the canonical hlTick cache-bust pattern:

       const x = React.useMemo(() => SomeStore.get(hlKey), [hlKey, hlTick]);

   hlTick is a bump-counter that increments on store mutations, signaling
   memos to re-read. ESLint flags it "unnecessary" because it can't see
   through the store-read boundary. Removing it breaks highlight/bookmark/
   note refresh.

   React 18's useSyncExternalStore replaces this idiomatically:

       const x = React.useSyncExternalStore(store.subscribe, () => store.get(hlKey));

   Each store grows `subscribe(cb) → unsubscribe` + `getSnapshot()`. Consumers
   replace tick-counter useMemo with useSyncExternalStore. No false-positive
   lint. Concurrent-renderer-safe.

   Scope: refactor — 6 stores + 16 consumer sites. Changes runtime re-render
   semantics (consumers re-render more precisely, not on any mutation).
   Q5 tests are now in place (per [[refactor-after-tests]]) — unblocked.

   Schedule: post-P7 (App() decomposition first), or alongside it if a
   P7 hook extraction naturally touches one of the 16 consumer sites.

3. BUNDLE-A.JS LAZY-LOAD (UX, post-Q6)
   ─────────────────────────────────────
   bundle-a.js is 11.7 MB loaded on every cold WebView boot. On mid-range
   Android, ~3-5 seconds of parse time before any UI paints.

   Split into:
     - bundle-a-core.js (~2 MB): vendor + search engine + small registries
     - bundle-a-corpus-{volKey}.js (lazy): each Volume/WTLB/etc.

   Load on-demand when user opens a collection. The single biggest UX win
   available. Significant refactor — needs COLLECTIONS registry to gate
   corpus loading + a fetch-and-await wrapper that doesn't break the
   synchronous `colLetterArr` contract.

   Schedule: post-Q6, deliberately scheduled (not a polish item — a real
   architectural change with smoke regression risk).

───────────────────────────────────────────────────────────────────────────────
INFRA / TOOLING ITEMS (one-liners — open small, ship fast)
───────────────────────────────────────────────────────────────────────────────

- lint:globals AUTO-REGEN in pre-commit hook: today the hook rebuilds bundles
  but does NOT run `npm run lint:globals`. If a dev adds a new global export,
  the committed globals file lags reality until CI regenerates it. Mirror
  the bundle-rebuild logic: regenerate + restage globals on any source change.

- LINT-STAGED auto-fix mode: currently `eslint --max-warnings 0` (no --fix).
  If devs want auto-fix on `--fix`-able warnings (unused-vars `_` prefix,
  whitespace), add a second config entry. Low priority — auto-fix can hide
  intentional patterns.

- `tsc --noEmit` IN CI ✅ DONE (Q4.1, `001747b`) — runs between lint and
  build with zero-tolerance. Pre-commit Step 3 mirrors the gate.

───────────────────────────────────────────────────────────────────────────────
USER POLICY REMINDERS (durable; mirror CLAUDE.md)
───────────────────────────────────────────────────────────────────────────────

- NO AI / NO API KEYS / NO LLM — deferred indefinitely (2026-05-11)
- NO credentials / login / auth anywhere; all data stays local on device
- NO security risks — anything that could leak personal data is a defect
- android:allowBackup="false" — Export/Import is the only backup mechanism
- App name is "VOTReader" (personal app; no Play Store thinking yet)

───────────────────────────────────────────────────────────────────────────────
END OF DOCUMENT — last pruned 2026-05-24 (Q3 closed)
───────────────────────────────────────────────────────────────────────────────
