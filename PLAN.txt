═══════════════════════════════════════════════════════════════════════════════
VOTReader-studio — Strategic Working Memory
═══════════════════════════════════════════════════════════════════════════════

Started: 2026-05-10
Last pruned: 2026-05-26 (PWA migration W0-W8 plan added; all prior phases CLOSED)
Status: ACTIVE — W0-W8 cross-platform + quality hardening plan in progress
Companions: CLAUDE.md (live status), HISTORY.md (landed work), ARCHITECTURE.md (system ref)

  ┌─────────────────────────────────────────────────────────────────────┐
  │ ►► ACTIVE WORK 2026-06-01: see UPLIFT-PLAN.txt — the "7/10 → 8/10"   │
  │ remediation from a deep 7-subsystem review (21 items U0–U21).        │
  │ NOTE: the "11.7 MB → 1.03 MB critical path" figures below are        │
  │ bundle-a ONLY; the real cold-boot blocking JS path is ~2.10 MB       │
  │ (a+b+c+d all load blocking). Corrected in UPLIFT-PLAN U0.            │
  └─────────────────────────────────────────────────────────────────────┘

───────────────────────────────────────────────────────────────────────────────
PURPOSE
───────────────────────────────────────────────────────────────────────────────

PLAN.txt is the strategic compass: what's next, what's deferred, and why.
It is NOT the live status doc (that's CLAUDE.md's "Current state" block) nor
the work log (that's HISTORY.md). Keep it small and high-signal — when an
item lands, move the prose to HISTORY.md and trim the entry here to a one-
liner referencing the commit.

The plan is intentionally phased so:
- Each phase is independently shippable
- Earlier phases reduce risk for later phases
- We never break working features mid-refactor

For the historical May-10 research-agent reports + the Phase 0-5 detailed
sprawl that produced JSX conversion + bundles + hooks extraction, see
HISTORY.md (specifically the Q2.7 + P6 + Objective G sections).

───────────────────────────────────────────────────────────────────────────────
PHASE STATUS — quality-uplift sequence (Q3 → Q4 → Q5 → Q6)
───────────────────────────────────────────────────────────────────────────────

  Q1 (CI day-one)             ✅ DONE  — `.github/workflows/ci.yml` build + syntax-check
  Q2 (modularization)         ✅ DONE  — 130+ ES modules, 4 cluster bundles, P6 hooks
  Q2.7 (JSX conversion)       ✅ DONE  — every component is JSX
  Q3 (ESLint)                 ✅ DONE  — 0 errors / 0 warnings, CI `--max-warnings 0`
                                          locked, lint-staged in pre-commit
  Q4 (JSDoc / tsc --checkJs)  ✅ DONE  — 37 files typed, CI gate zero-tolerance
  smoke-lite.js for CI        ✅ DONE  — Node CLI; globals + collections + cycles
  Q5 (Vitest)                 ✅ DONE  — 218 tests / 9 files; ratchet 1→14 stmts
  App() decomposition (P7)    ✅ DONE  — Phase 1 (logic, P7a-k) + Phase 2
                                          (render-tree, P9a-g). App() 1,815
                                          → 797 lines.
  Q6 (CSS hardening)          ✅ DONE  — app.css 4,410 → 4,125. 93 hex → vars.
                                          Cat A !important removed (11 of 36).
                                          See CLAUDE.md "Q6".
  Q7 (useSyncExternalStore)   ✅ DONE  — 23/24 Bin 4 hlTick disables removed.
                                          7 stores + 14 consumers migrated.
                                          476 vitest tests. See CLAUDE.md "Q7".
  Bundle-a.js lazy-load (Q8)  ✅ DONE  — Q8.1 books (ea94158) +
                                          Q8.2 matthew (dcd06c3) + Q8.3
                                          all VOT corpora (5605f30). Critical
                                          path 11.7 MB → 1.03 MB (91%).
  N1 native-side polish       ✅ DONE  — 13 commits (f61bb43..ff0f459):
                                          10 N1.x items + 3 post-review
                                          hardening commits (d8d0ab6 /
                                          1ea0127 / ff0f459) closing real
                                          correctness paths.
                                          See CLAUDE.md "N1" section.

───────────────────────────────────────────────────────────────────────────────
POST-Q4 SEQUENCING (executed 2026-05-24)
───────────────────────────────────────────────────────────────────────────────

The pinned order (1) smoke-lite → (2) Q5 Vitest → (3) App() decomposition was
executed in that order. Rationale: high-risk structural refactors come AFTER
test coverage on the affected surface (memory [[refactor-after-tests]]).

  1. smoke-lite.js ✅  Node-runnable safety net for CI; vitest imports
                       runSmokeChecks for module-graph + globals assertions
  2. Q5 Vitest     ✅  218 tests / 9 files; coverage ratchet locked at the
                       Q5 baselines, bumped to 14/11/12/15 floors at Q5.9
  3. App() decomp  ✅  Phase 1 (P7a-k) + Phase 2 (P9a-g) closed 2026-05-25.
                       App() 1,815 → 797 lines.

───────────────────────────────────────────────────────────────────────────────
POST-Q4 BACKLOG (priority order — top = next)
───────────────────────────────────────────────────────────────────────────────

1. APP() DECOMPOSITION (P7) — CLOSED 2026-05-25
   ─────────────────────────────────────────────
   Phase 1 (logic, hook extraction) and Phase 2 (render-tree decomposition)
   both landed. App.jsx: 1,815 → 797 lines. 26 hooks (15 P6 + 11 P7a-k).
   53-entry ROUTES dispatch table now lives in src/ui/screen-routes.jsx
   via buildScreenRoutes(deps) factory. 3 substantive ROUTES entries
   (matthew-ch, bible-study-chapter, holy-days-index) extracted to their
   own screen / component files. AppShellOverlays + AppShellSheets host
   the 4 modal overlays and 12 sheets/popovers respectively. All 5
   pre-commit gates pass; 465 vitest tests passing. Per-commit breakdown
   in CLAUDE.md "Phase 2" section + git log (P7a-k, P8a-c, P9a-g).

2. BUNDLE-A.JS LAZY-LOAD — Q8 CLOSED (2026-05-25)
   ──────────────────────────────────────────────
   Cold-boot critical path: 11.7 MB → 1.03 MB (91% reduction) across
   three commits.

   Q8.1 (ea94158) books.js → bundle-a-bible.js (6.9 MB lazy).
   Q8.2 (dcd06c3) matthew.js → bundle-a-matthew.js (618 KB lazy).
                  Factored loader into __makeLazyLoader helper.
   Q8.3 (5605f30) all 14 VOT corpora → bundle-a-vot.js (3 MB lazy).
                  __finishVotInit re-runs linkWtlbEntries +
                  linkPreface + VOT_LETTER_REGISTRY rebuild on load.

   See CLAUDE.md "Q8" section + HISTORY.md for the per-commit
   implementation details + smoke walk results.

3. NATIVE-SIDE POLISH — N1 CLOSED (2026-05-25)
   ──────────────────────────────────────────────
   13 commits bring MainActivity.kt to the same quality bar as the JS
   side. 10 N1.x items: Timber logging, onRenderProcessGone recovery,
   PixelCopy screenshots on a coroutine path, type-safe JsBridge for
   every evaluateJavascript site, per-frame keyboard tracking,
   ViewModel state, and the recorder + storage logic extracted into
   focused classes.

   Plus 3 post-review hardening commits that closed real correctness
   paths uncovered by a critical review after N1.10b landed:
     d8d0ab6  N1.3 retry-path dangling webView field
     1ea0127  N1.7 PixelCopy bitmap recycle race
     ff0f459  N1.10b queryFileSize exception escape

   Final commit range f61bb43..ff0f459. MainActivity 869 → 937 lines
   (net growth from added functionality; recorder + storage extracted
   to their own files). New files: VOTReaderApp, JsBridge,
   MainViewModel, NativeAudioRecorder, StorageManager. New deps:
   Timber 5.0.1, coroutines 1.10.2, lifecycle 2.9.1 (runtime +
   viewmodel).

   Real-device verification owed for: DevTools attach, renderer crash
   recovery (including retry-view branch), PixelCopy on
   hardware-accelerated content, keyboard animation smoothness,
   rotation mid-recording, full smoke walk.

   See CLAUDE.md "N1" section + HISTORY.md for the per-commit detail.

═══════════════════════════════════════════════════════════════════════════════
PWA MIGRATION — CROSS-PLATFORM (Android + PC)
═══════════════════════════════════════════════════════════════════════════════

Goal: VOTReader runs as a native Android APK (existing) AND as an installable
PWA on desktop (Chrome/Edge/Firefox). One JS codebase, two shells. All data
stays local on both platforms. No server, no credentials, no API keys.

Rationale — the inversion:

  The architectural review surfaced 18 critiques. The deepest one — "WebView
  fighting native UX" — is the meta-critique behind most of the others.
  Going cross-platform doesn't just ADD a platform. It INVERTS that critique.
  The 135+ modules of battle-tested web code stop being a limitation locked
  inside a single-platform Android shell and become the core asset. PWA
  validates the foundation rather than rebuilding it.

  Concretely, every strength W0-W8 leans on is already in place:
    - AndroidBridge call sites are already guarded with `if (window.AndroidBridge)`
      — desktop browser mostly works today (W1 is formalization, not invention).
    - N1's native extraction (BridgeHost / AppInterface / JsBridge /
      MainViewModel / NativeAudioRecorder / StorageManager) already demonstrates
      the shell-vs-app separation that PlatformBridge will mirror on the JS side.
    - The 6-step pre-commit gate + 476 vitest + 134 JUnit + JaCoCo 0.85 floor
      means changes are inspectable per-commit. The safety net is built;
      this work uses it rather than rebuilding it.
    - 1.03 MB critical-path bundle is small enough to cache as the PWA
      install-time shell (W3).

  One codebase, two shells, all data local. No server, no credentials, no API
  keys, no runtime network egress. The PWA isn't a port — it's revealing what
  was already there.

  W0-W6 makes the app work everywhere. W7-W8 closes every remaining critique.
  When W8 closes, the 18-item traceability matrix at the bottom of this file
  is clean — zero gaps.

  Mandate: not just executing the plan. Stay alert to anything broken or
  missable as we touch each file. Improve as we go. Creative & professional.
  Every commit ships to production.

Current state (pre-migration):
  - The JS app ALREADY mostly works in a desktop browser (React, stores,
    corpus, rendering all load). AndroidBridge calls silently no-op because
    every call site is already guarded: `if (window.AndroidBridge) {...}`.
  - What DOESN'T work on desktop: file import/export, audio recording,
    native screenshots, back-button navigation, fullscreen toggle, zoom,
    haptics, status bar, keep-screen-on.
  - Font files are declared in CSS @font-face but DO NOT EXIST in the repo
    (fonts/ directory is empty). This is a pre-existing visual bug on any
    platform — system fallback fonts render instead of Cinzel/Garamond.
  - localStorage persistence works on both but has quota ceiling (~5-10 MB)
    and no transactional writes.
  - No PWA infrastructure exists: no manifest.json, no service worker, no
    offline fallback, no PWA meta tags.

Sequencing rationale:
  W0 → W1 → W2 → W3 → W4 → W5 → W6

  W0 (prerequisites) unblocks everything: fonts + owed device verification.
  W1 (platform bridge) makes desktop functionally correct — file I/O, audio,
    back-button, screenshots all work via browser APIs.
  W2 (IndexedDB) fixes the persistence ceiling on BOTH platforms — the one
    change with highest user-facing impact.
  W3 (PWA shell) makes it installable + offline on desktop.
  W4 (desktop polish) optimizes the desktop experience after it works.
  W5 (hosting + CI) ships it.
  W6 (cross-platform verification) proves it.

  Each phase is independently shippable. W1 alone makes the app usable on
  desktop. W2 alone improves both platforms. Nothing breaks mid-migration
  because every phase is additive — the Android path continues to work
  unchanged throughout.

───────────────────────────────────────────────────────────────────────────────
────────────────────────
W0–W5 — PREREQUISITES · PLATFORM BRIDGE · STORAGE · PWA SHELL · DESKTOP · HOSTING — ALL LANDED
────────────────────────

  These six phases are CLOSED, CI-green, and deployed. Their detailed plan
  bodies (intent, exit criteria, sub-phase breakdowns — ~2,000 lines) were
  executed, then removed 2026-06-01 (D7 doc-prune) to slim this strategic-
  memory file. Landed outcomes are logged in HISTORY.md (W1, W2, W2-polish,
  W3, W4, W5 + the Briefing-archived block); planning rationale lives in
  `git log -- PLAN.txt`.

W6 — CROSS-PLATFORM VERIFICATION (prove it)
───────────────────────────────────────────────────────────────────────────────

The final gate. Both platforms get a full smoke walk + targeted
regression testing for the migration surfaces.

  W6.1 — Android regression walk
  ──────────────────────────────
  Full NK7 checklist (287 items) on a physical device. Verify:
    - All N1.x native features still work after PlatformBridge refactor
    - IndexedDB migration from localStorage completed on first boot
    - Data integrity: annotations, notes, bookmarks, journal all intact
    - Performance: no boot-time regression from IndexedDB hydration
    - Service worker NOT registered (web-only gate working)

  W6.2 — Desktop smoke walk (Chrome + Edge + Firefox)
  ───────────────────────────────────────────────────
  Per-browser walk covering:
    - PWA install + offline access
    - All 53 screens render
    - File import/export via browser APIs
    - Audio recording + playback
    - Annotation CRUD (highlight, underline, note)
    - Search (full-text across all corpora)
    - Back navigation via Escape + browser back button
    - Keyboard shortcuts
    - Responsive layout at 1440px, 1024px, 768px widths
    - Dark/light theme toggle
    - Service worker update cycle

  W6.3 — Automated cross-platform smoke (stretch goal)
  ────────────────────────────────────────────────────
  Add playwright tests that run the smoke walk headlessly in CI.
  Not required for launch but valuable for regression prevention.
  Covers: boot → navigate screens → annotate → search → export.

  W6.4 — Owed native fixes (implement here — they need a device to verify)
  ────────────────────────────────────────────────────────────────────────
  Verified accurate against the Kotlin 2026-05-29:
    #1 Export on Android 8/9 (minSdk=26) — DONE 2026-05-31 (not a fallback
       — a full replacement). The old StorageManager.writeJsonToDownloads
       returned Failure("requires_android_10") below API 29, so Android 8/9
       users COULD NOT export — and export is the only backup
       (allowBackup=false), i.e. real data-loss exposure for in-target
       devices. Replaced the MediaStore.Downloads writer with SAF
       ACTION_CREATE_DOCUMENT (API 19+, works on every supported device AND
       lets the user choose the destination folder/filename):
       AppInterface.saveToFile (async, fire-and-forget) → BridgeHost
       .launchExportPicker → MainActivity.exportPickerLauncher
       (ActivityResultContracts.CreateDocument) → StorageManager
       .writeTextToUri(uri, content); outcome to JS via
       window.__onExportComplete (new JsEvent.ExportComplete) =
       "ok"/"error:<reason>"/"cancelled". Web keeps the Blob+anchor
       download, now also firing __onExportComplete. No new permission
       (SAF is permission-free). Data completeness + compression were
       already correct (all stores + all media base64'd; images maxDim
       1600/q0.8, audio AAC 96k/opus) — left untouched. tsc + 1512 vitest
       + Kotlin testDebugUnitTest/JaCoCo all green; web path verified
       end-to-end in preview. DEVICE-VERIFIED 2026-06-01 on an API-28
       (Android 9) emulator (a6389d0): Export → ACTION_CREATE_DOCUMENT →
       DocumentsUI picker (pre-filled filename) → SAVE wrote a complete v2
       payload to /sdcard/Download. (Verifying this is ALSO what surfaced
       the Android 8/9 black-screen bug — see the WebView-floor note below.)
    #2 Screenshot async — optional polish. captureScreenshot does
       runBlocking on a binder thread (2s-capped). Make the JS side a
       Promise resolved via evaluateJavascript when PixelCopy finishes.
       LOW urgency: bounded, not concurrent, and the sync contract was a
       deliberate N1.7 choice ([[explicit-async-decision]]).
    #3 Garden native image cache (Android) — DONE 2026-05-31 (9483500).
       Was also the fix for per-page-turn LAG, not just data-usage: the
       GitHub release URL 302-redirects to a no-cache signed asset, so the
       WebView re-downloaded every image + re-did the redirect hop on every
       navigation (invisible on desktop's fast pipe; a stall on a phone).
       JS can't cache them (no CORS → fetch fails) and the PWA SW never
       runs on Android, so it's native: GardenImageCache in MainActivity's
       shouldInterceptRequest serves from / populates cacheDir/garden/,
       KEYED BY PAGE NUMBER (tier stripped) so a quality change overwrites
       rather than accumulating copies (≤209 files). Cap 800 MB — measured
       Ultra at ~3.5 MB/page avg (~720 MB full read), so a full read of any
       tier incl. Ultra never evicts mid-browse (an initial 260 MB cap was
       observed churning Ultra and was raised). Atomic writes; failures
       degrade to null (WebView loads normally). Device-verified on Ultra:
       accumulated to 176 files / 586 MB with zero eviction + zero errors;
       6 GardenImageCacheTest cases. App data, not in export / "your data".

  Exit criteria:
    [ ] Android 287-item walk: all PASS
    [ ] Chrome desktop walk: all PASS
    [ ] Edge desktop walk: all PASS
    [ ] Firefox desktop walk: all PASS
    [ ] Data migrated correctly on both platforms
    [ ] Zero regressions from pre-migration baseline
    [ ] Android 8/9 export works via the SAF picker (#1) — CODE DONE
        2026-05-31; device-verify on a genuine API 26-28 device still owed

  Risk: LOW (verification, not implementation).

───────────────────────────────────────────────────────────────────────────────
────────────────────────
W7–W9 — CODE QUALITY · TYPE COVERAGE/CSS · DATA INTEGRITY — ALL LANDED
────────────────────────

  CLOSED, CI-green, deployed. Detailed plan bodies executed, then removed
  2026-06-01 (D7 doc-prune); landed outcomes are in HISTORY.md (W7, W8, W9
  sections). See `git log -- PLAN.txt` if the planning rationale is needed.

W10 — ACCESSIBILITY (WCAG 2.1 AA compliance)
───────────────────────────────────────────────────────────────────────────────

  *** SCOPE DECISION (2026-05-31): SHIP W10-LITE, DEFER THE FULL AA SUITE. ***
  Audience = 20+ readers, all sighted/hearing/able-bodied, some older. The
  full plan below (W10.1-W10.5) is overwhelmingly screen-reader + keyboard-
  only work with ZERO in-audience recipients. The audience-matched subset is
  defined in the W10-LITE block (right after "Current state"): text-size
  control + motion/visual (W10.5) + contrast/touch-target audits.

Goal (FULL SUITE — DEFERRED 2026-05-31; see W10-LITE below): make VOTReader fully usable by keyboard-only users, screen reader
users, and users with motion/visual sensitivities. Target: WCAG 2.1 AA
across all 53 screens, 12 sheets, 4 modal overlays, and the annotation
toolbar.

Current state (audited 2026-05-28):
  - 94 aria-* attributes across 39 files (scattered, not systematic)
  - 33 role= attributes across 16 production files (mostly JournalViewer)
  - 0 role="dialog" or aria-modal="true" on ANY modal/sheet/overlay
  - 0 focus traps — Tab escapes every modal into background content
  - 0 focus-return-to-trigger on any modal close
  - 0 prefers-reduced-motion references (CSS or JS)
  - 0 .sr-only / screen-reader-only CSS classes
  - 1 <label> element in the entire app (SettingsRow)
  - 6 tabIndex usages (ad hoc, not managed)
  - 8 .focus() calls (ad hoc, no focus management utility)
  - 0 focus trap / focus management utilities
  - SelectionToolbar (733 lines, 12+ buttons): zero aria-label on any
    button, zero role="toolbar", zero keyboard navigation within the
    toolbar, color buttons have title but are visually empty to screen
    readers
  - AppShellOverlays: zero ARIA on any overlay div, welcome modal close
    button has no accessible name, garden warning has no radio-group
    semantics
  - AppShellSheets: zero role="dialog" on any of the 12 sheet overlays
  - Toast notifications: no aria-live region
  - ConfirmStrip: no aria-live for dynamic confirmation questions

  ┌──────────────────────────────────────────────────────────────────┐
  │ W10-LITE — AUDIENCE-MATCHED SCOPE (RECOMMENDED, 2026-05-31)        │
  │ Ship this. Defer everything in W10.1-W10.5 not listed here.        │
  └──────────────────────────────────────────────────────────────────┘

  STATUS — SHIPPED 2026-06-03. All W10-lite items landed; the full
  W10.1-W10.5 AA suite stays DEFERRED per the rationale in this block.
    WL1  Text Size control ........ DONE (da050c8) — Settings selector +
         --font-scale root multiplier; journal-styles.js (87) +
         HighlightsScreen.jsx (14) px->rem for uniform scaling; lsShim +
         boot script apply the persisted scale pre-mount (no FOUC). Preview-
         verified set->live->persist->cold-boot on chrome + reading surface;
         0 horizontal overflow at 375px/1.5x; 1878 vitest pass.
    WL2  Motion + visual ........... DONE — reduced-motion + :focus-visible
         landed in the prior W10-lite pass; WL2c forced-colors (highlight
         color preservation in Windows High Contrast) added (0cd9075).
    WL3  Contrast audit ........... DONE (prior pass) — dark theme AA-clean;
         light --link-blue darkened to clear AA (3.15->4.60:1).
    WL4  Touch-target audit ....... DONE (0cd9075) — app already AA (24px:
         color dots/style btns/journal/nav all clear); the one sub-24px
         control (storage-banner dismiss) given a 44px hit area via an
         ::after overlay. AA-compliant 24-30px targets deliberately NOT
         pushed to AAA 44px (risk vs. an able-bodied audience; WL1 scaling
         already enlarges text targets).
    WL5  Glyph aria-labels ........ DONE (prior pass) — icon-only buttons
         labelled. Remaining screen-reader/role work stays deferred.

  WHY LITE: audience = 20+ readers, all sighted/hearing/able-bodied, some
  older. Screen-reader semantics (W10.4), modal focus traps (W10.2), and
  keyboard-only nav + roving tabindex (W10.1d/W10.3) serve populations with
  ZERO members in this audience — and carry real regression risk on a working
  app (focus traps fight NoteSheet autofocus + ConfirmStrip; div->h1 shifts
  CSS; roving tabindex on the 733-line SelectionToolbar is fiddly). The only
  in-audience need is OLDER READERS: bigger text, stronger contrast, bigger
  tap targets, less motion. That is an ergonomics project, which W10-as-scoped
  barely addresses — the #1 item (WL1) isn't even in the original W10 plan.

  WL1 — TEXT-SIZE CONTROL   [the highest-value item; NOT in original W10]
  ────────────────────────────────────────────────────────────────────
    Stepped size selector in Settings (Text & Translation section, under
    "Modern Fonts"). Persist in vot-state.settings.fontScale. Boot script
    (index.html) applies it pre-mount like fontStyle (no FOUC); use-settings.js
    mirrors it live on toggle.
    Mechanism: a --font-scale CSS var multiplied into the root rem (or scale
    the root font-size directly). Steps e.g. 1.0 / 1.15 / 1.3 / 1.5. This is
    NOT the 260-declaration font-family migration documented in the FONT
    TOGGLE section — that migration is for font CHOICE (swapping typefaces);
    SIZE is a separate axis that only needs the root multiplier.

    COST LOCK (measured 2026-05-31):
      - app.css: 364 rem font-sizes vs 2 px. Root-scale covers all 364
        cleanly; fix the 2 px stragglers.
      - The PRIMARY reading surfaces (letters / Bible chapters / WTLB entries /
        search results) are all app.css rem -> they scale for free.
      - BUT two JS-injected style subsystems are 100% px and will NOT scale
        with a root multiplier (they're injected via R('...') template strings,
        not app.css):
          * styles/journal-styles.js        — 87 px font-sizes, 0 rem
          * ui/screens/HighlightsScreen.jsx — ~14 px font-sizes (its <style>)
        (+ a journal-stats toast glyph and 4 close/back button glyphs ×/‹ —
        icon glyphs, leave them.)
      - To make scaling UNIFORM, convert those ~101 px->rem (÷16) in the two
        files. Bounded, mechanical, CSS-only, visual-diffable in preview. This
        is the one non-trivial piece of WL1.
      - >>> DECISION NEEDED before WL1 impl: convert the two px subsystems
        (uniform scaling everywhere) OR ship without (reading surfaces scale;
        Journal + Highlights index stay fixed-size). RECOMMEND: convert — so an
        older reader doesn't hit two screens that silently ignore their size
        setting.

  WL2 — MOTION + VISUAL   (= all of W10.5; highest value-per-line in the phase)
  ────────────────────────────────────────────────────────────────────
    Take W10.5a/b/c verbatim — all cheap, global, broadly helpful:
      - prefers-reduced-motion (one nuclear CSS rule; helps vestibular-
        sensitive older users)
      - :focus-visible outlines (also helps older mouse users who lose focus)
      - forced-colors annotation preservation

  WL3 — CONTRAST SPOT-AUDIT
  ────────────────────────────────────────────────────────────────────
    Older eyes lose contrast sensitivity. Audit body text + the GOLD accents
    (verse numbers, chrome, ~#e8c050 on dark) against WCAG AA (4.5:1 body /
    3:1 large) in BOTH dark + light mode. Gold-on-dark at small sizes is the
    likely failure. Fix by nudging the gold value or size — not a bandaid rule.

  WL4 — TOUCH-TARGET SPOT-AUDIT
  ────────────────────────────────────────────────────────────────────
    Age reduces tap precision even in able-bodied hands. Enforce a 44x44px
    floor (WCAG 2.5.5) on: bottom-nav buttons, footnote bubbles, color
    swatches, SelectionToolbar buttons, close/back glyph buttons. Pad the
    hit-area; visible size can stay the same.

  WL5 — OPTIONAL CHEAP CHERRY-PICK   (low cost; future-proofs if audience broadens)
  ────────────────────────────────────────────────────────────────────
    aria-label on icon-only buttons + role="dialog" on modals — LABELS ONLY,
    no focus-trap machinery, no behavior change. Skip everything else screen-
    reader-shaped. Grab if cheap; not required for lite.

  DEFERRED (record as a decision, not a silent gap):
    All of W10.1 (focus trap / announce / roving tabindex), W10.2 (modal focus
    mgmt), W10.3 (keyboard-only nav), W10.4 (screen-reader semantics), and the
    native crash-view announceForAccessibility. Reason: no in-audience users.
    The full suite below stays as the reference if the audience ever broadens.

  ─── FULL SUITE BELOW (W10.1-W10.5) — DEFERRED per the W10-LITE decision ───

  W10.1 — Accessibility infrastructure (foundation primitives)
  ────────────────────────────────────────────────────────────
  Build the reusable primitives that W10.2-W10.5 consume. Pure additions
  — zero behavior change to existing UI.

  W10.1a — useFocusTrap hook:
    New file: src/hooks/use-focus-trap.js (~80 lines)

    useFocusTrap(containerRef, active)
      - On activate:
        1. Store document.activeElement as returnTarget
        2. Query all focusable elements in containerRef (FOCUSABLE_SELECTOR:
           'a[href], button:not(:disabled), input:not(:disabled),
           textarea:not(:disabled), select:not(:disabled),
           [tabindex]:not([tabindex="-1"])')
        3. Focus the first focusable element (or the container itself if
           none found, with tabIndex={-1} on the container)
      - On Tab keydown (within container):
        If Shift+Tab on first focusable → wrap to last
        If Tab on last focusable → wrap to first
        (The sheet's Escape handler from useModalRegistry still fires
        independently — focus trapping and dismiss are separate concerns)
      - On deactivate:
        returnTarget.focus() if returnTarget is still in the DOM
        (handles the case where the trigger was removed while the modal
        was open — e.g. annotation action chip trigger disappears after
        the annotation is deleted)

    Tests: vitest with jsdom — trap wraps on Tab, wraps on Shift-Tab,
    restores focus on deactivate, handles empty container, handles
    removed trigger.

  W10.1b — sr-only CSS class:
    Add to app.css:
      .sr-only {
        position: absolute; width: 1px; height: 1px;
        padding: 0; margin: -1px; overflow: hidden;
        clip: rect(0, 0, 0, 0); white-space: nowrap;
        border: 0;
      }
    Standard screen-reader-only pattern. Used by W10.3 for icon-only
    buttons that need accessible text, and by W10.4 for off-screen
    announcements.

  W10.1c — aria-live announcement utility:
    New file: src/utils/announce.js (~30 lines)

    announce(message, priority = 'polite')
      - Creates/reuses a persistent <div role="status" aria-live={priority}
        aria-atomic="true"> off-screen (sr-only styled)
      - Sets textContent = '' → microtask → textContent = message
        (the clear-then-set forces screen readers to re-announce even if
        the message text is identical to the previous announcement)
      - Used by: toast notifications, ConfirmStrip state changes,
        store mutation feedback (bookmark added, note saved, etc.)

    Tests: vitest — element created on first call, reused on subsequent,
    textContent set correctly, polite vs assertive priority.

  W10.1d — useRovingTabindex hook:
    New file: src/hooks/use-roving-tabindex.js (~60 lines)

    useRovingTabindex(items, activeIndex, setActiveIndex)
      - Returns props to spread on each item: { tabIndex, onKeyDown, ref }
      - Arrow keys (Left/Right or Up/Down based on orientation param)
        move activeIndex with wrapping
      - Home/End jump to first/last
      - Active item gets tabIndex={0}, rest get tabIndex={-1}
      - On arrow key: .focus() the newly active item

    Used by: SelectionToolbar buttons, color palette, garden tier
    selector, any toolbar or button group.

    Tests: vitest — arrow key wraps, Home/End, focus follows active index.

  W10.2 — Modal/sheet ARIA + focus management
  ───────────────────────────────────────────
  Wire the W10.1 primitives into every modal, sheet, and overlay in the
  app. This is the highest-impact tier — it's the difference between
  "keyboard users are trapped behind a modal they can't escape" and
  "the app works."

  Sequencing: mechanical (same pattern repeated N times), so batch by
  risk ascending per [[low-risk-pilot-first]].

  W10.2a — Sheet overlay pattern (pilot on NoteSheet):
    For each sheet:
      1. Add role="dialog" and aria-modal="true" to the sheet panel div
      2. Add aria-label={descriptive title} (e.g. "Edit note", "Create
         bookmark", "Pick notebook")
      3. Call useFocusTrap(sheetRef, true) — active whenever the sheet
         is mounted
      4. Verify: focus trapped, Escape still dismisses (useModalRegistry
         unaffected), focus returns to trigger on close

    NoteSheet is the pilot because:
      - It has an existing autoFocus on the textarea (proves focus
        trap doesn't fight autoFocus)
      - It has a ConfirmStrip (proves focus trap handles dynamic
        sub-content)
      - It has 3 modes (read/edit/color) — stress-tests focus
        management across mode transitions

  W10.2b — Remaining sheets (11):
    Apply the same pattern to all remaining sheets:
      AnnotationActionChip, BookmarkCreateSheet, BookmarkPopover,
      LinkSidebar, LinkPicker, ScriptureSheet, NotebookPickerSheet,
      JournalRecordingSheet, JournalInboundSheet, JournalInsertSheet,
      TabActionSheet

    Special cases:
      - AnnotationActionChip: floating chip, not full-screen overlay.
        aria-label="Annotation actions". Focus trap still applies —
        Tab should wrap within the 3-4 visible buttons, not escape to
        the reading content behind.
      - LinkSidebar: side panel, not bottom sheet. role="complementary"
        instead of "dialog" (it's a supplementary panel, not a blocking
        modal). Focus trap optional — user should be able to Tab between
        sidebar and main content. Use aria-label="Linked content".
      - JournalRecordingSheet: has a live timer and amplitude bar.
        Add aria-live="polite" to the timer display so screen readers
        announce elapsed time updates (throttled to every 10 seconds
        to avoid chattering).

  W10.2c — Modal overlays (4):
    Apply to AppShellOverlays:
      Welcome modal: role="dialog", aria-modal="true",
        aria-label="Welcome to VOTReader". Close button gets
        aria-label="Close welcome message". useFocusTrap.
      Tabs overview: role="dialog", aria-label="Open tabs".
      Disable-tabs prompt: role="alertdialog" (it's a confirmation),
        aria-label="Disable tabs feature".
      Garden warning: role="dialog", aria-label="Garden access".
        Tier selector buttons → role="radiogroup" on container,
        role="radio" + aria-checked on each button.

  W10.2d — Type-DELETE confirmation modal (SettingsScreen):
    The "Clear All Personal Data" modal:
      role="alertdialog", aria-modal="true",
      aria-label="Confirm delete all personal data".
      The input already has aria-label. Add aria-describedby pointing
      to the warning text paragraph. useFocusTrap.

  W10.2e — Safari/StorageHealth modals (3):
    Safari7DayModal: role="alertdialog", aria-label="Safari storage
      warning". Focus trap. Buttons get aria-label.
    IosPwaWelcomeCard: role="dialog", aria-label="Import data from
      Safari". Focus trap.
    StorageHealthBanner: already has role="alert" (verified). Add
      aria-live="polite" if not already present. Dismiss button
      needs aria-label="Dismiss storage warning".

  Tests per tier:
    - vitest: role + aria attributes present in rendered output
    - vitest: focus trap activation/deactivation
    - Manual: keyboard-only walkthrough of open → interact → close
      cycle for each modal/sheet

  W10.3 — Keyboard navigation (toolbars, palettes, reading view)
  ─────────────────────────────────────────────────────────────
  Make every interactive surface keyboard-navigable beyond just Tab.

  W10.3a — SelectionToolbar (733 lines, highest-impact):
    Current state: 12+ buttons, zero keyboard nav, zero ARIA.

    Add role="toolbar" to the toolbar container.
    Add aria-label="Text annotation tools".
    Add aria-label to every button:
      - Highlight colors: aria-label="Yellow highlight" (etc.)
      - Style toggle: aria-pressed={isUnderline}, aria-label="Underline
        style" / "Highlight style"
      - Action buttons: aria-label matching the visible text label
        (Note, Link, Copy, Share, Search, Bookmark)
      - Remove: aria-label="Remove highlight"

    Wire useRovingTabindex for Left/Right arrow navigation between the
    action buttons. Color palette gets its own nested roving group.

    ConfirmStrip ("Remove this highlight?"): add aria-live="assertive"
    to the strip container so the question is announced when it appears.

  W10.3b — Color palette (shared pattern):
    The 10-color annotation palette appears in SelectionToolbar and
    NoteSheet. Each color button:
      - aria-label="[Color name] highlight color"
      - aria-pressed={isSelected} (for the currently active color)
      - useRovingTabindex for arrow key navigation within the palette

  W10.3c — Settings screen controls:
    Toggle switches: add role="switch" + aria-checked to each toggle.
    The existing SettingsRow <label> element is the only label in the
    app — extend the pattern to all settings rows.
    Clear progress buttons: aria-label describing what will be cleared.
    Font toggle: role="radiogroup" with role="radio" + aria-checked
    on Classic/Modern buttons.

  W10.3d — Navigation chrome:
    Bottom nav bar: role="navigation", aria-label="Main navigation".
    Each nav button: aria-label + aria-current="page" when active.
    Back pill: aria-label="Go back to [previous screen name]".
    Search bar: <label> element (sr-only) + aria-label on input.

  W10.3e — Reading view keyboard shortcuts:
    Content screens (letter view, chapter view, WTLB entry):
    - Left/Right arrow: prev/next chapter/letter (matching the swipe
      gesture). Gated on: not in an input/textarea, not in a modal.
    - Space/Shift+Space: scroll page (browser default — just ensure
      nothing intercepts it).
    - Home/End: scroll to top/bottom of content.
    These are progressive enhancements — they add keyboard access to
    existing touch gestures without changing any visible UI.

  W10.4 — Screen reader semantics (landmarks, headings, announcements)
  ──────────────────────────────────────────────────────────────────
  Make the app navigable by landmark and heading for screen reader users.

  W10.4a — Landmark roles:
    App shell: role="main" on the content area (the ROUTES render area).
    Nav bar: role="navigation" (from W10.3d).
    Search results: role="search" on the SearchScreen container.
    Settings: role="form" on the settings content area.

  W10.4b — Heading hierarchy:
    Every screen title → <h1> (or aria-level="1" if the element can't
    be an h1). Currently most screen titles are <div> or <span> with
    CSS styling. Changing to <h1> gives screen readers a navigation
    anchor.
    Section headings within screens → <h2>.
    Sub-sections → <h3>.
    Rule: exactly one <h1> per screen, always the screen title.

  W10.4c — Dynamic announcements:
    Wire the W10.1c announce() utility into state changes:
      - "Bookmark added" / "Bookmark removed" (BookmarkIcon toggle)
      - "Highlight applied" / "Highlight removed" (SelectionToolbar)
      - "Note saved" / "Note deleted" (NoteSheet)
      - "Link created" / "Link removed" (LinkPicker)
      - "Copied to clipboard" (copy action)
      - Toast messages (already visual — now also announced)
      - Navigation: "Navigated to [screen name]" on screen change
        (helps screen reader users orient after navigation)

  W10.4d — Content accessibility:
    Reading content (letter blocks, Bible verses, WTLB entries):
      - Poetry blocks: role="group" with aria-label="Poetry"
      - Scripture blocks: role="group" with aria-label="Scripture quote"
      - Footnote bubbles: aria-label="Footnote [number]" + role="button"
      - Verse numbers: aria-label="Verse [number]" (currently decorative
        gold superscripts with no accessible text)
      - Inline scripture refs (Format B {{ref:...}}): role="button",
        aria-label="View [reference]"

  W10.5 — Motion and visual accessibility
  ───────────────────────────────────────
  Respect user preferences for reduced motion, high contrast, and
  focus visibility.

  W10.5a — prefers-reduced-motion:
    Current state: ZERO references in the codebase. All CSS animations
    (sheet slide-up, overlay fade-in, tab transitions, toast appear)
    run unconditionally.

    Add to app.css:
      @media (prefers-reduced-motion: reduce) {
        *, *::before, *::after {
          animation-duration: 0.01ms !important;
          animation-iteration-count: 1 !important;
          transition-duration: 0.01ms !important;
          scroll-behavior: auto !important;
        }
      }

    This is the nuclear option — kills ALL animations for users who
    prefer reduced motion. It's the safest starting point because:
      1. It's one rule, not 30 per-animation overrides
      2. It respects the user's system setting unconditionally
      3. It can't miss a new animation added later
      4. The 0.01ms (not 0ms) preserves animation-end events that
         JS code may depend on (prevents logic breakage)

    If specific animations should survive for reduced-motion users
    (e.g., a subtle opacity fade is acceptable), carve out exceptions
    AFTER the nuclear rule lands and is verified.

  W10.5b — Focus-visible indicators:
    Current state: browser defaults. Some browsers show outlines on
    click (annoying for mouse users) and others suppress them entirely
    (invisible for keyboard users).

    Add to app.css:
      :focus-visible {
        outline: 2px solid var(--hl-yellow);
        outline-offset: 2px;
      }
      :focus:not(:focus-visible) {
        outline: none;
      }

    This shows outlines ONLY for keyboard navigation (not mouse clicks).
    The outline color uses the existing --hl-yellow var for visual
    consistency with the annotation system. Dark mode: --hl-yellow is
    already visible on dark backgrounds. Light mode: same.

  W10.5c — Forced colors / high contrast:
    Add to app.css:
      @media (forced-colors: active) {
        .hl-note, .hl-underline { forced-color-adjust: none; }
      }

    This preserves annotation colors in Windows High Contrast mode.
    Without it, all highlight colors collapse to the system highlight
    color (usually blue), making the 10-color palette meaningless.

    Other elements (buttons, nav, text) should FOLLOW forced-colors
    to respect the user's contrast needs. Only annotation highlights
    opt out because color IS the information.

  Tests for W10.5:
    - vitest: matchMedia mock for prefers-reduced-motion → verify
      animation properties resolve to 0.01ms
    - Visual: toggle "Reduce motion" in OS settings, verify all
      animations suppressed
    - Visual: toggle High Contrast, verify annotations retain colors
      while chrome follows system palette
    - Visual: keyboard-tab through the app, verify focus ring visible
      on every interactive element

  Exit criteria (W10 complete):
    [ ] Native renderer-crash retry view (MainActivity): move the
        "Tap to reload" string to strings.xml + announceForAccessibility()
        on show, so a screen-reader user hears the crash-recovery prompt.
        (#3, verified 2026-05-29 — refines the proposal: a contentDescription
        on a TextView is redundant since its visible text is already its
        accessible name; the real gap is the view isn't ANNOUNCED when the
        WebView process dies.)
    [ ] useFocusTrap hook: focus wraps within every modal/sheet,
        returns to trigger on close. vitest coverage.
    [ ] announce() utility: all user-feedback actions announced for
        screen readers. vitest coverage.
    [ ] useRovingTabindex hook: arrow-key navigation in toolbars and
        palettes. vitest coverage.
    [ ] Every modal/sheet: role="dialog"|"alertdialog", aria-modal,
        aria-label, focus trap, focus return. 0 exceptions.
    [ ] SelectionToolbar: role="toolbar", every button has aria-label,
        keyboard navigation between buttons. vitest coverage.
    [ ] Navigation chrome: landmark roles, aria-current on active tab.
    [ ] Every screen title is an <h1> (or equivalent). Exactly one
        per screen.
    [ ] All state-change feedback announced (bookmarks, highlights,
        notes, links, navigation, toasts).
    [ ] prefers-reduced-motion: all animations suppressed.
    [ ] Focus-visible: keyboard outlines on all interactive elements,
        no outlines on mouse click.
    [ ] Forced colors: annotation highlights preserved.
    [ ] Keyboard-only walkthrough of ALL 53 screens + 12 sheets + 4
        overlays — every action achievable without a mouse.
    [ ] Screen reader walkthrough (NVDA or VoiceOver): every screen
        navigable by landmark + heading, every action announced,
        every modal trapped.

───────────────────────────────────────────────────────────────────────────────
SEQUENCING + PARALLELISM
───────────────────────────────────────────────────────────────────────────────

  Critical path: W0 → W1 → W2 → W3 → W5 → W6 (cross-platform launch).
  Then W7 + W8 (quality hardening).
  Then W9 + W10 (integrity + accessibility).

  Parallelism opportunities:
    W4 (desktop polish) can run alongside W3 or W5.
    W7.1-W7.2 (schema + raw freeze) can start during W2 (same store layer).
    W7.3 (hlTick) can run anytime after W2 (no dependency).
    W7.4 (diagnostics) can run anytime after W1 (needs PlatformBridge).
    W7.5 (prop bundles) must precede W8.1 Tier 3 (screens need bundles).
    W8.1 (UI typing) can start Tier 1-2 during W6 (leaf components first).
    W8.2 (CSS @layer) — RETIRED 2026-05-31 (net-negative; see W8.2 section).
    W8.3 (scripture types) is small — slot anywhere.
    W9.1-W9.2 (schema defs + build validation) independent — can run
      anytime. No dependency on any other phase.
    W9.3 (import validation) benefits from W7.1 (schema versioning) but
      doesn't strictly depend on it — can run independently.
    W9.4 (pre-commit hook) needs W9.2 (build validation).
    W10.1 (a11y primitives) independent — can start during W8 or after.
    W10.2 (modal ARIA + focus) needs W10.1 primitives.
    W10.3 (keyboard nav) needs W10.1d (useRovingTabindex). Benefits from
      W8.1 (typed components make aria-* prop checking easier) but can
      run without.
    W10.4 (screen reader) needs W10.1c (announce utility). Independent
      of W10.2 and W10.3.
    W10.5 (motion/visual) fully independent — can run first or last.

  Recommended execution order for W9 + W10:
    W9.1 → W9.2 → W9.4 (schema pipeline — sequential)
    W9.3 (import validation — can run in parallel with W9.2)
    W10.1 → W10.2 → W10.3 (a11y — sequential, each builds on prior)
    W10.4 + W10.5 (can run in parallel with W10.3 or after)

───────────────────────────────────────────────────────────────────────────────
RISK REGISTER
───────────────────────────────────────────────────────────────────────────────

  HIGH — IndexedDB sync render-path (W2 Tier 3)
    The 5 stores read synchronously in render paths (annotations, notes,
    links, state, prophecy-cards) must be in-memory before React renders.
    The hydration gate is the mitigation, but boot-time latency may
    increase. Measure: time from page load to first render, compare
    pre/post migration. Target: < 200ms overhead.

  HIGH — Safari 7-day data eviction (W2.7 Scenario 3)
    Safari evicts all script-writable storage after 7 days without user
    interaction. Home-screen PWAs are exempt. Users who create data in
    a Safari tab without installing will lose everything. Mitigation:
    intercept first data-creating gesture with a modal explaining the
    risk + install instructions. Can't fix the platform constraint —
    can only warn clearly enough that the user acts before data loss.

  MEDIUM — Export/import media round-trip (W2.6)
    exportPersonalData currently drops all journal media blobs (audio,
    images). Fix adds async base64 encoding of all blobs into the JSON
    payload. For users with >100 MB of media, this may OOM or produce
    a multi-hundred-MB JSON file. Mitigation: 100 MB guard warns user;
    ZIP-based streaming export deferred to W7.

  MEDIUM — iOS PWA storage isolation (W2.4 + W2.7 Scenario 4)
    Installing a PWA on iOS creates a NEW storage container. Data from
    the Safari tab is invisible. Users who build up data in Safari then
    install lose access to it. Mitigation: detect empty-storage-on-
    first-PWA-boot and show an export/import bridge card. Can't fix
    the platform constraint — Apple isolates by design.

  MEDIUM — Budget Android near-zero quota (W2.7 Scenario 7)
    Chrome/WebView quota = 60% of FREE disk. Devices with <500 MB free
    get quotas as low as a few MB. App must detect and warn, not crash.
    Mitigation: startup health check + QuotaExceededError handling on
    every write path → graceful degradation to read-only mode.

  MEDIUM — Audio recording cross-browser (W1.4)
    MediaRecorder API: Chrome supports audio/webm, Firefox supports
    audio/ogg. Safari doesn't support MediaRecorder reliably. Need
    to test mime negotiation and base64 encoding on all three browsers.
    Mitigation: mime detection at start time, fallback to audio/wav
    if needed.

  MEDIUM — Service worker cache size (~12.5 MB total)
    Full corpus cache is large. Some browsers may throttle or reject.
    Mitigation: cache critical path on install (~2.3 MB), lazy bundles
    on first fetch. Monitor navigator.storage.estimate() for pressure.

  MEDIUM — hlTick removal blast radius (W7.3)
    39 files touched. The 8 truly-live uses must be migrated to
    useSyncExternalStore before the plumbing can be removed. If any
    live use is missed, annotation re-renders will silently stop
    working. Mitigation: smoke walk every annotation operation (add,
    remove, color change, note create, bookmark toggle, link add)
    after W7.3a before starting W7.3b.

  (RETIRED) CSS @layer cascade rewrite (W8.2)
    Risk no longer applies — W8.2 retired 2026-05-31 (the @layer rewrite
    was judged net-negative: @layer fails closed on an old WebView, for
    zero user-facing gain). The 5 redundant !important that WERE removed
    were proven no-ops via computed-style probes, not a cascade rewrite.

  LOW — CSP compatibility on GitHub Pages
    'self' should resolve correctly. If not, fall back to explicit
    origin in CSP. Easy to diagnose via console errors.

  LOW — Font licensing
    Cinzel + EB Garamond are OFL (SIL Open Font License). Bundling
    in an APK and serving via GitHub Pages are both permitted uses.
    No license risk.

  LOW — UI typing flood (W8.1)
    82 files × ~5 type errors each = ~400 initial errors. But Q4
    proved the pattern: file-by-file, 4-7 files/day, zero-tolerance
    after each. Risk is tedium, not breakage.

  MEDIUM — Schema validation false positives (W9.1-W9.2)
    The 21 data files evolved organically over months. Some structural
    patterns may be intentional but look like violations to a strict
    schema (e.g., Hidden Manna has only 1 letter, Holy Days entries
    borrow structure from Format B but have extra fields like
    sourceLabel). Mitigation: run validators against ALL current data
    files BEFORE declaring schemas final. Any false positive = schema
    bug, not data bug. warnings vs errors distinction ensures
    intentional deviations don't block the build.

  LOW — Import validation rejects valid V1 exports (W9.3)
    Users with pre-W2 V1 export files import through a backward-compat
    path that parses LS JSON strings. The shape validators must handle
    V1's stringified format, not just V2's native objects. Mitigation:
    validators run AFTER the V1→V2 parsing step, not before. Test with
    a real V1 export payload.

  MEDIUM — Focus trap interactions with existing keyboard handling (W10.2)
    The app already has: useModalRegistry (Escape dispatch), history-sync
    (popstate), Escape key listener (6 gates), ConfirmStrip registration.
    Adding focus traps creates a new keyboard interceptor that must
    coexist. Specifically: Tab/Shift-Tab trapping in a sheet must NOT
    interfere with Escape dismissal (different keys, should be fine) but
    MUST handle the case where ConfirmStrip appears inside a trapped
    sheet (the trap's focusable-element query must re-run when the
    strip mounts/unmounts). Mitigation: pilot on NoteSheet (has
    ConfirmStrip + textarea + mode transitions) before rolling out.

  LOW — Heading hierarchy retrofit (W10.4b)
    Changing screen titles from <div>/<span> to <h1> may affect CSS
    rules that target element type (e.g., `div.screen-title`). Audit
    finds: screen titles are styled via class selectors, not element
    selectors. Low risk. Mitigation: visual smoke walk after each
    screen's heading change.

───────────────────────────────────────────────────────────────────────────────
CRITIQUE TRACEABILITY MATRIX
───────────────────────────────────────────────────────────────────────────────

Every critique from the architectural review + the 2026-05-28 app
review is mapped to a specific work item. Zero gaps.

  | # | Critique                              | Phase    | Status     |
  |---|---------------------------------------|----------|------------|
  | 1 | WebView fighting native UX            | W1       | INVERTED   |
  |   |                                       |          | (PWA       |
  |   |                                       |          | validates) |
  | 2 | localStorage quota ceiling            | W2       | RESOLVED   |
  | 3 | localStorage partial-write corruption | W2       | RESOLVED   |
  | 4 | No error boundary in App              | W2.8     | RESOLVED   |
  | 5 | Window bridge informal globals        | W1       | RESOLVED   |
  | 6 | No offline/installable desktop        | W3       | RESOLVED   |
  | 7 | Missing font files                    | W0.1     | RESOLVED   |
  | 8 | Owed real-device verification         | W0.2+W6  | RESOLVED   |
  | 9 | No quota monitoring                   | W2.5     | RESOLVED   |
  | 10| Desktop UX (layout, hover, keyboard)  | W4       | RESOLVED   |
  | 11| Store schema versioning               | W7.1     | RESOLVED   |
  | 12| raw() returns live mutable object     | W7.2     | RESOLVED   |
  | 13| hlTick dead prop threading            | W7.3     | RESOLVED   |
  | 14| No JS-side telemetry/diagnostics      | W7.4     | RESOLVED   |
  | 15| buildScreenRoutes 47-prop surface     | W7.5     | RESOLVED   |
  | 16| UI tree untyped (82 files)            | W8.1     | RESOLVED   |
  | 17| !important (most load-bearing)        | W8.2     | RETIRED    |
  | 18| Scripture parsing lacks JSDoc types    | W8.3     | RESOLVED   |
  | 19| Data fragility (structural validation)| W9.1-9.2 | PLANNED   |
  | 20| Import corruption vector              | W9.3     | PLANNED    |
  | 21| No accessibility (focus/ARIA/keyboard) | W10      | PLANNED    |
  | 22| No prefers-reduced-motion support     | W10.5a   | PLANNED    |
  | 23| No focus-visible indicators           | W10.5b   | PLANNED    |
  | 24| No screen reader landmarks/headings   | W10.4    | PLANNED    |

───────────────────────────────────────────────────────────────────────────────
INFRA / TOOLING ITEMS (one-liners — open small, ship fast)
───────────────────────────────────────────────────────────────────────────────

- lint:globals AUTO-REGEN in pre-commit hook: today the hook rebuilds bundles
  but does NOT run `npm run lint:globals`. If a dev adds a new global export,
  the committed globals file lags reality until CI regenerates it. Mirror
  the bundle-rebuild logic: regenerate + restage globals on any source change.

- LINT-STAGED auto-fix mode: currently `eslint --max-warnings 0` (no --fix).
  If devs want auto-fix on `--fix`-able warnings (unused-vars `_` prefix,
  whitespace), add a second config entry. Low priority — auto-fix can hide
  intentional patterns.

- `tsc --noEmit` IN CI ✅ DONE (Q4.1, `001747b`) — runs between lint and
  build with zero-tolerance. Pre-commit Step 3 mirrors the gate.

───────────────────────────────────────────────────────────────────────────────
PROPOSED — GW1: Service-Worker Garden image cache (PWA parity) — DESIGN ONLY, NOT STARTED
───────────────────────────────────────────────────────────────────────────────

STATUS: architected 2026-05-31, NOT executed. The Android APK already has a
native per-page Garden disk cache (W6.4 #3 / GardenImageCache.kt, shipped
9483500). This phase brings the SAME behavior to the INSTALLED PWA (desktop
Chrome/Edge/Firefox + Android Chrome PWA), which today has no Garden cache —
it relies on the browser's volatile HTTP/image cache, so a PWA user gets the
same per-page-turn re-download + redirect-hop the native app used to.

WHY IT'S A SEPARATE PHASE (not just "reuse the native cache"):
  - The SW only runs in the PWA. sw-register.js:31 hard-returns on Android
    (PlatformBridge.isAndroid) — the APK bundles assets + uses the native
    WebView path, never the SW. So this is strictly additive for PWA users;
    it cannot affect and is not affected by the APK.
  - The native cache (Kotlin, cacheDir, page-keyed) and the SW cache (JS,
    CacheStorage, page-keyed) are independent implementations of one policy.
    Document the shared invariants once (below) so they can't drift.

THE CORE CONSTRAINT (why it's not trivial — same as the native side):
  GitHub release URLs 302-redirect (Cache-Control: no-cache) to a per-request
  SIGNED release-assets.githubusercontent.com URL, and the assets carry NO
  CORS headers. Consequences for a SW:
    - The SW CAN cache the image as an OPAQUE response (no CORS needed for
      cache.put of an opaque Response; the <img> tag consumes it fine).
    - The SW must cache under a NORMALIZED page key, NOT the request URL —
      the signed URL differs per request AND per tier, so URL-keyed caching
      would never hit + would store N copies. Use cache.put(Request(pageKey),
      opaqueResponse) where pageKey = a synthetic same-origin URL like
      `/__garden/042` derived from the garden_NNN.jpg filename (tier stripped).
    - Opaque responses have status 0 + unknown size; Cache Storage stores
      them but they count ~7× their real size against the origin quota in
      some engines (the "opaque response padding" — important for the cap).

SHARED INVARIANTS (must match GardenImageCache.kt — single policy, two impls):
  1. KEY BY PAGE NUMBER ONLY (strip the tier). Re-reading at a new quality
     OVERWRITES the same page → never N copies per tier. Count bounded ≤209.
  2. Cap sized ABOVE a full Ultra read so a cover-to-cover read of any tier
     never evicts mid-browse. Native uses 800 MB of real bytes; the SW must
     account for opaque-padding (a 720 MB real Ultra book may bill as multi-GB
     against quota) — so the SW cap is in ENTRY COUNT (≤209 + small margin)
     and/or checks navigator.storage.estimate() pressure, NOT a raw byte cap.
  3. Eviction = drop the least-recently-served page when over the count cap.
     Never evict the current ± preload-ahead window (GARDEN_PRELOAD_AHEAD=5).
  4. Garden cache is APP DATA: a 4th SW cache bucket `vot-garden-<v>`, NEVER
     cleared by export, NEVER counted in "Your data" (user-data-size.js
     already excludes it by omission — keep it out of USER_DATA_STORES). It
     IS counted in "Total app data" (estimate().usage) automatically.

DESIGN:
  W-GW1.a — SW fetch handler for Garden URLs:
    In service-worker.js, the fetch listener currently early-returns on
    cross-origin (line ~135). Add, BEFORE that return, a check: if the URL
    matches the garden release pattern (github.com/.../releases/download/
    garden-<tier>/garden_NNN.jpg OR the release-assets redirect carrying
    garden_NNN in rscd), route to gardenFirst(request, pageNum).
      gardenFirst:
        - key = new Request(`${scope}__garden/${pageNum}`)  // normalized
        - hit = await gardenCache.match(key); if hit → return hit
        - else: res = await fetch(request)  // follows 302; opaque is fine
                if (res && (res.ok || res.type === 'opaque'))
                  gardenCache.put(key, res.clone()); enforceCountCap()
                return res
        - catch → return fetch(request) bare (never block the image)
    Cross-origin opaque caching requires the request reach the SW: the
    redirect chain does (the initial github.com GET is what the <img> fires).
    Confirm in the W-GW1 spike that the SW sees garden_NNN before the 302.

  W-GW1.b — count-based eviction (opaque-padding-safe):
    enforceCountCap(): list gardenCache keys; if > CAP (e.g. 220 — 209 pages
    + headroom), delete oldest-inserted beyond the cap. Track insertion order
    via a tiny IDB/meta list or by a timestamp header added to the cached
    Response (SW can synthesize headers on put). Simpler: since key is
    page-numbered, on overflow just delete keys not in the live window —
    but the crawl fills all 209, so prefer LRU-by-insertion. RESOLVE in spike.

  W-GW1.c — cache version + lifecycle:
    New bucket constant GARDEN_CACHE = `vot-garden-${GARDEN_ASSET_VERSION}`.
    GARDEN_ASSET_VERSION bumps ONLY if the Garden source images change
    (rare). The activate handler's existing cleanup loop gains a
    `vot-garden-` branch to drop old versions. Pre-cache nothing on install
    (209 Ultra images = ~720 MB — never auto-download; cache-on-view only,
    exactly like the corpus-cache-on-first-fetch pattern at corpusFirst()).

  W-GW1.d — settings integration (optional, nice-to-have):
    A "Clear Garden image cache" affordance + a size readout. The SW exposes
    cache size via postMessage; Settings shows it under "Total app data"
    detail. LOW priority — the OS already evicts cacheDir/CacheStorage under
    pressure, and it's regenerable.

RISKS / OPEN QUESTIONS for the spike (resolve BEFORE building):
  - Does the SW reliably intercept the github.com request BEFORE the 302,
    given <img> cross-origin loads? (Service workers DO see no-cors <img>
    subresource requests, but verify the redirect is followed inside the SW
    fetch, not by the browser opaquely.) If the browser follows the 302
    before the SW sees a cacheable response, may need to cache the FINAL
    release-assets URL response instead — still keyed by normalized page.
  - Opaque response quota padding: confirm 209 Ultra opaque entries don't
    blow the origin quota on a low-storage desktop. estimate() pressure check
    is the mitigation.
  - Interaction with the existing in-memory gardenImageCache + the crawl:
    the SW is transparent to both (they still fire <img> loads; the SW just
    serves them faster). No JS change needed in GardenView — same as native.

VERIFICATION (when built): preview/desktop Chrome with SW — navigate Garden,
confirm 2nd view of a page is served from CacheStorage (DevTools → Application
→ Cache Storage shows vot-garden-* with /__garden/NNN keys, one per page);
switch tier, confirm the SAME key is overwritten (count stays ≤209); offline
reload still shows visited pages. Cross-browser: Chrome + Edge + Firefox.

EFFORT: medium. One file (service-worker.js) + a constant + the activate
branch + a spike to resolve the redirect-interception question. No native
code, no bundle change. Independent of everything; ship anytime.

───────────────────────────────────────────────────────────────────────────────
USER POLICY REMINDERS (durable; mirror CLAUDE.md)
───────────────────────────────────────────────────────────────────────────────

- NO AI / NO API KEYS / NO LLM — deferred indefinitely (2026-05-11)
- NO credentials / login / auth anywhere; all data stays local on device
- NO security risks — anything that could leak personal data is a defect
- android:allowBackup="false" — Export/Import is the only backup mechanism
- App name is "VOTReader" (personal app; no Play Store thinking yet)
- CROSS-PLATFORM: same JS codebase, Android APK + PWA desktop. No server.

───────────────────────────────────────────────────────────────────────────────
END OF DOCUMENT — last updated 2026-05-28 (W0-W10 complete plan: PWA + quality hardening + data integrity + accessibility — all 24 critiques resolved)
───────────────────────────────────────────────────────────────────────────────
