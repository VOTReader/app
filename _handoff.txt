HANDOFF INSTRUCTIONS — VOTReader-studio items 6-48 fix pass
============================================================
Written: 2026-05-09 while at 38% context window.
Read CLAUDE.md Section 19.3 for full status table.

WHAT WAS DONE (20 items fixed)
------------------------------
All edits in: D:\VOTReader-studio\app\src\main\assets\index.html
              D:\VOTReader-studio\app\src\main\assets\search-data.js

1. Item 8+9: bookCategory() — hoisted _OT_BOOKS_INLINE Set to module scope (~line 3137).
   bookCategory() now uses OT_BOOK_IDS (from BIBLE_BOOK_LIST) when available, falls back
   to _OT_BOOKS_INLINE for early-load. No more per-call Set construction.

2. Item 10: tabField() — stable setter refs via _tabSetters cache + _uatRef (~line 10157).
   Setters are created once per key and reused. _uatRef.current always points to latest
   updateActiveTab so closures don't go stale.

3. Item 11: useEffect([commitDwellNow]) — added dependency array to __onDwellCommit effect.

4. Item 12: __closeSheet stacking — ChapterView (~6611), WtlbEntryView (~7345), and
   LetterView (~7652) all converted to save/restore pattern:
     var prev = window.__closeSheet;
     window.__closeSheet = closer;
     return () => {window.__closeSheet = prev || null;};
   SelectField and TabActionSheet were already correct.

5. Item 13: navOrigin — goSettings/goHistory now capture {studyId, studyChapterId}.
   goNavOrigin now restores them. Settings->back from bible-study-chapter now works.

6. Item 15: useMarkAsRead hook — defined near CachedStore (~line 2857). Replaced 4
   identical useEffect blocks in ChapterView, WtlbEntryView, LetterView, PdfPageView.

7. Item 18: Dead matthewTile — removed `const matthewTile = null` and all 4 render
   sites in ScripturesHome (genre grid, compact list, book grid, canonical scroll).

8. Item 19: findBook collision — renamed local in describeTab() from findBook to
   resolveBook. Module-scope findBook() unchanged.

9. Item 21: search-data.js — removed SYNONYM_GROUPS, SYNONYM_MAP, stemWord, phoneticKey
   from window.VotSearchData exports. Definitions kept in file (harmless dead code).

10. Item 22: parseInt radix — added ", 10" to ALL 11 parseInt calls:
    parseRefStr (3), buildSourceEndpoint (2), findEntryContext (2),
    parseRefRanges (2), renderLine/navMatch (2).

11. Item 23: CachedStore._save catch — changed from `catch(e){}` to
    `catch(e){console.warn('localStorage write failed for', storageKey, e)}`

12. Item 24-28, 30: CSS cleanup:
    - Removed duplicate .section-block/.section-heading (kept canonical at ~1676)
    - Commented out 3 empty rules (.letter-list-item, .letter-body, .search-screen)
    - Removed 4 unused @keyframes (fadeUp, fadeIn, homeHeroIn, homeOrnIn)
    - Removed --bg4 and --study-bg from both :root and body.light
    - Simplified --gold-border-faint to --gold-border
    - Added --link-blue and --accent-pink CSS vars to :root + body.light
    - Replaced all #6cb4dc (4x) with var(--link-blue)
    - Replaced all #f48fb1 (3x) with var(--accent-pink)
    - Replaced #e6dece (3x) with var(--bg3) in body.light rules
    - Replaced #ede7d8 (1x) with var(--bg2) in body.light rule

13. Item 40: fromLetterStack — added 50-entry cap in pushFromLetter.

WHAT WAS DEFERRED (23 items)
-----------------------------
See CLAUDE.md Section 19.3 "Reviewed, deferred with rationale" table for full details.

Key ones to know about:
- Item 6: LETTERS naming in volume-two.js — not worth the mechanical rename
- Items 14,16,17: Component extractions (InlineNotes/StudyPanels, BottomNav, ScriptureSheet)
  — abstraction cost exceeds dedup benefit in pre-compiled React.createElement code
- Item 43: setInterval polls — replacing requires refactoring __scrollEl global to pub/sub
- Items 41-42: Accessibility (focus traps, keyboard nav) — complex, no regression risk
- Items 34-39: Data format cosmetic issues — no functional impact

VERIFICATION STATUS
-------------------
- App loads clean: YES (preview_start on port 8090, zero console errors)
- Home screen: all 5 cards render
- Volume Two index: all 29 letters render
- Letter view: "The Wide Path" loads with 3 footnotes
- bookCategory(): genesis=OT, matthew=NT, psalms=OT, revelation=NT
- parseRefStr("1 Corinthians 7:32-35"): chapter=7, verse=32, verseEnd=35
- CLAUDE.md updated with full fix/defer status table

HOW TO VERIFY IF SOMETHING BROKE
---------------------------------
1. Open preview: preview_start name="app" (port 8090)
2. Reload: preview_eval "window.location.reload()"
3. Check errors: preview_console_logs level="error"
4. Test navigation: Home -> Volumes -> V2 -> letter -> back -> Settings -> back
5. Test study nav: Home -> Studies -> Matthew Study -> Settings -> back (tests item 13)
6. Test bookCategory: preview_eval "bookCategory('genesis')" should return "Old Testament"

KEY FILE LOCATIONS
------------------
- Main app: D:\VOTReader-studio\app\src\main\assets\index.html (~13,100 lines)
- Search data: D:\VOTReader-studio\app\src\main\assets\search-data.js
- Project docs: D:\VOTReader-studio\CLAUDE.md (Section 19.3 has full status)
- Preview config: D:\VOTReader-studio\.claude\launch.json
- ALWAYS edit D:\ files, never C:\ (junction in place, see CLAUDE.md 17.11)

IMPORTANT PATTERNS TO PRESERVE
-------------------------------
- useMarkAsRead(enabled, callback) — new hook, used in 4 components
- _tabSetters ref cache — do NOT add new tabField() calls without understanding this
- __closeSheet save/restore — all 5 sites now use the same pattern
- CSS variables --link-blue, --accent-pink — use these instead of raw hex
- _OT_BOOKS_INLINE fallback — exists because bookCategory() is called before
  OT_BOOK_IDS is defined (OT_BOOK_IDS is built from BIBLE_BOOK_LIST at ~line 9568)
