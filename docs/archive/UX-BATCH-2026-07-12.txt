═══════════════════════════════════════════════════════════════════════════
UX-BATCH-2026-07-12 — owner-directed UX batch (9 items → 5 sessions)
═══════════════════════════════════════════════════════════════════════════
Owner directive (2026-07-12): a batch of UX requests. "Suggestions are
flexible — improve/redesign where you know better. Pay special attention to
UX/UI; everything intuitive, natural, self-explanatory; ⓘ info buttons where
useful. Act like a professional, expert designer."

This file is the canonical tracker. Mark items DONE with a dated one-liner.
Each session: recon-verify current code (docs drift) → implement → RED-first
tests where fixing behavior → gates (vitest, lint --max-warnings 0, typecheck,
smoke:ci) → preview-verify at 375px → commit+push → update CLAUDE.md handoff.

───────────────────────────────────────────────────────────────────────────
SESSION 1 — Scripture references: "Go to verse" + back pill  [THIS SESSION] ✅ DONE
───────────────────────────────────────────────────────────────────────────
Owner: "Tap footnote scripture references… or any other way a scripture is
referenced… add some kind of button to go to where the actual scripture is
in the app, with the proper scripture highlighted with the behavior we
already have built, also make android back feature aware and add back to
pill at top."

RECON FINDINGS (all verified in source 2026-07-12):
- The scripture-reference SHEET surfaces (each shows a ref + verse text):
    1. FootnoteSheet.jsx        — Format A numbered footnotes (LetterView +
                                  BibleStudyChapterView via LetterView shim)
    2. ScriptureSheet.jsx       — Matthew study cite sheets (MATTHEW_NKJV)
    3. LetterView.jsx inline    — inline scripture-ref sheet (SC5)
    4. WtlbEntryView.jsx inline — Format B {{ref:}} / (Book C:V) cite sheet
  (Journal viewer {{ref:}} links already NAVIGATE directly — 3f271ce.)
- Verse-highlight nav behavior EXISTS: navigateToLink (use-navigate-to-link.js)
  routes {type:'bible',bookId,chapter,verse} → bible-ch/matthew-ch and sets
  surpriseAnchor {type:'verse',verses:[…]} → BibleChapterView flash-highlights
  4s + scrolls (same for Matthew).
- Back pill EXISTS: useFromLetterStack backHint → "Back to <title>" pill on
  destination; navigateToLink pushes the source entry (with destSnapshot
  pruning). Android back consults fromLetterStack (use-android-back).
- parseRefStr/findBook handle ref→book/chapter/verse(+verseEnd); the journal
  viewer's __loadBibleCorpus-retry pattern handles the lazy corpus.

PLAN:
- Shared pure helper (scripture-resolution.js): resolve a ref string →
  {endpoint:{type:'bible',bookId,chapter,verse,verseEnd}} or null.
- Extend navigateToLink: verses array spans verse..verseEnd (range refs
  highlight the whole range, not just the first verse).
- Add a clearly-labeled "Open in <Book C:V> →"-style gold action button to
  all 4 sheets (shared CSS class); hidden when the ref can't resolve to an
  in-app book. Tap → close sheet → navigateToLink with meta
  {sourceLetterTitle, sourceVolumeLabel} so the pill reads "Back to <letter>".
- Corpus-lazy safe: kick __loadBibleCorpus and navigate on resolve (never
  drop the tap) — navigateToLink already does this for bible endpoints.
- Android back: free via fromLetterStack (verify).
DONE 2026-07-12: new shared GoToRefButton (ui/components, globalized) on all
4 sheets — reuses the .fn-sheet-link-btn family ("Go to Scripture" eyebrow +
ref title + chevron). COMPOUND Matthew study cites ("Psalm 118:14; Isaiah
12:2") render one button PER passage. verseAnchorFor in use-navigate-to-link
makes range refs flash the WHOLE span (capped 176). Mount effect pre-warms
__loadBibleCorpus; unresolvable taps retry 40×250ms (journal {{ref:}}
pattern). NOTE: findBook("Matthew")='matthew-plain' → refs open the PLAIN
Bible Matthew (consistent w/ journal nav); study endpoints (bookId
'matthew') still route matthew-ch. Preview-proven: letter fn → Proverbs
16:17 flash+pill+1-press android back; Matthew 27:15-26 range → 12 verses
flashed; WTLB {{ref:}} → flash+pill-tap return; study compound cite →
Isaiah 12:2 + "Back to Study Bible · Matthew 1". Gates: 2529 vitest/140
files (+21), lint 438 globals, typecheck, smoke:ci PASS, 0 console errors.
FOLLOW-UP (owner: "don't see the flash"): .verse-surprise had NO CSS rule
ANYWHERE — the flash was always invisible (Surprise button included); the
scroll masked it. Fixed in app.css: gold wash + 5px spread + 4px radius on
.verse-surprise (light-theme variant), base transition on .verse/.verse-row
fades the flash in AND the 4s clear out; under prefers-reduced-motion the
global 0.01ms override degrades to a static 4s highlight (transition-based
deliberately — a keyframes+forwards approach would compute to TRANSPARENT
under that override). Preview: computed rgba(200,164,86,0.22)+spread proven
(hidden preview tab freezes transitions — verify computed style with
transition:none, not by eye).
FOLLOW-UP 2 (owner: "lit-up square, not the text"): the wash moved OFF the
block container onto the verse's inner [data-hl-key] span (HighlightableText
root — inline, wraps exactly the verse text, hugs line boxes like a real
highlighter) and now reuses the letters' tap-through look verbatim: same
--hl-* palette vars + the letter-highlight-pulse double-pulse keyframes
(single 8.4s run — both pulses land inside the 4s window), light-theme
variant included. Container styling fully removed. Verified: inner span
inline + rgba(200,164,86,0.38) base + letter-highlight-pulse animation,
container transparent.

───────────────────────────────────────────────────────────────────────────
SESSION 2 — Link picker overhaul (items 3 + 6 + 7) ✅ DONE 2026-07-13
───────────────────────────────────────────────────────────────────────────
LANDED (owner also reported: "search only searches titles and chapters" —
confirmed, searchNavIndex is titles/refs only; full-text added):
- LinkPicker has THREE MODES via a segmented control: [Search] [Browse]
  [Recent]. All feed the same create/refine pipeline, so journal card/
  excerpt insert modes get every mode for free.
- SEARCH has an explicit SCOPE TOGGLE (owner follow-up): "Titles & refs"
  (nav-index — the default; refs like "Eph 6:5" live here) vs "Full text"
  (MiniSearch over every verse + letter/entry body; VotSearchMini in
  bundle-e — picker mount kicks __loadScreensE + a deferred engine init).
  Full-text results map to nav items via contentDocToNavItem (nav-index.js;
  Hidden Manna maps to null → can never surface), render with snippets,
  cap 20; verse hits are verse-precise → direct create in link mode. The
  no-titles-match empty state offers a one-tap "Search the full text
  instead" jump; placeholders + empty states are scope-specific.
- BROWSE: new buildNavTree (nav-index.js) groups the index into the app's
  own hierarchy — The Holy Bible → 66 books → chapter GRID; Matthew Study
  Bible → chapters; every letters collection → entries; each Bible study →
  chapters. Breadcrumb ‹ back per level. The STUDY 'matthew' bookId is
  excluded from the Bible book list (it has its own root — was showing two
  identical "Matthew" rows).
- RECENT: the link network — LinkStore.all() newest-first as From ⇄ To
  endpoint chips; tapping either endpoint reuses that exact (already-
  refined) place as the new target in ONE tap (no re-refinement).
- LATENT BUG FIXED: buildNavIndex cached FOREVER on first call — built
  before a lazy corpus landed, it permanently lost whole corpora (e.g. no
  Bible chapters in the picker until app reload). Now signature-guarded
  (books+collections+studies+matthew counts) and rebuilds when corpora
  arrive; the picker mount kicks all loaders.
- VERSE PICKER: got the same document-selectionchange fix as the excerpt
  picker (handle drags fire no page touch events) + a live VERSE FILTER
  ("Find a verse by its words…", count badge, tap-number still selects).
- EXCERPT PICKER: "Find in this letter…" bar — match count + ‹ › cycling,
  current hit gets a gold wash + scroll-to-center; nothing hidden (the
  full letter stays selectable).
Preview-proven at 375px on the real corpus: phrase "my cup runs over" (no
title match) → Psalms 23:5 top hit with snippet → verse picker → filter
"anoint" = 1 of 6 → footer enabled; Browse walked Bible→Psalms→150-grid→
ch23→verse picker, all 23 roots present; Recent chips created a real link
in one tap (+ Undo verified); find bar cycled 1 of 5 → 2 of 5 with the
wash moving. 0 console errors. Gates: 2567 vitest / 144 files (+16),
lint(440 globals — contentDocToNavItem + buildNavTree), typecheck,
smoke:ci PASS.
NOTE (deferred): the picker's content search indexes under the DEFAULT
translation (nkjv) — fine for finding places; the main Search screen still
owns translation-aware indexing.
OLD PLAN (for reference):
Owner items:
  (3) "Network of links — link picker has optional second tab/screen showing
      targets and sources of recent links."
  (6) "Add search function to link picker."
  (7) "Full top-down menu (regular menu navigation) as a toggleable way to
      get to your desired target in link picker, instead of just recent
      targets or search."

RECON: LinkPicker.jsx ALREADY has search (searchNavIndex over the nav index,
30 results) + a Recent list (RecentNavStore). So item 6 is interpreted as:
search must exist at EVERY step of the flow — notably the refinement pickers
(VersePickerScreen: long chapters; LetterExcerptPickerScreen: long letters)
have no filter/search today. ⚠ FLAGGED to owner in session summary.

PLAN:
- Segmented mode control in the picker: [Search] [Browse] [Recent Links].
  - Browse: full hierarchical drill-down mirroring app nav (Volumes → letters;
    WTLB parts → entries; Bible → books → chapters; Studies → chapters;
    Blessed/Holy Days), reusing the nav-index data. Breadcrumb + back per level.
  - Recent Links: the link NETWORK — recently created links as source ⇄ target
    rows (from LinkStore, newest first); tapping an endpoint picks it as the
    new target (and can jump-preview). This doubles as "recent links" browse.
  - Search: existing behavior (default mode).
- Add lightweight filter/search inside VersePicker + LetterExcerptPicker.
- Keep the create/refine/success-strip flow byte-identical otherwise.

───────────────────────────────────────────────────────────────────────────
SESSION 3 — Journal editor: insert-below + menu/nav redesign (items 8 + 9)
✅ DONE 2026-07-14 — plus the owner's two new device reports (long-entry
drag difficulty + the Android "scroll jerks back / won't scroll / restart
required" glitch), root-caused to THREE compounding defects and fixed:
(a) AUTOSIZE REBUILT — every render re-ran each textarea's JS resize
    (height:auto → scrollHeight), forcing a layout at the COLLAPSED height
    that clamped the scroller's scrollTop on long entries (fires per
    keystroke AND at the 1.2s Saving…→Saved re-render = deterministic
    jerk-back). Now the CSS grid-replica technique (.jrn-grow wrapper,
    ::after renders data-rep invisibly in the same grid cell; typography on
    the WRAPPER so textarea/replica can never drift) — zero JS measuring.
    Live-proven: every textarea clientH === scrollH (up to 1326px/47
    lines); scrollTop pixel-identical through typing + the debounce
    re-render.
(b) PER-ENTRY SCROLL KEYS — journal-viewer/journal-editor shared ONE
    scroll-memory slot ('journal-editor'), so opening entry B force-
    restored entry A's offset while the 90-frame restore loop re-applied
    it against the user's finger. getScrollKey now keys journal screens
    per-entry (journalEntryId param threaded App→useScrollMemory, added to
    the restore-effect deps so entry→entry nav restores correctly). +3
    scroll-memory tests.
(c) ZOMBIE WATCHDOG in press-drag (ALL four drag surfaces) — a silently-
    killed pointer stream (no up, no cancel — the documented device
    failure) left the gesture live forever: its non-passive touchmove
    suppressor ate EVERY scroll and the editor's autoscroll loop kept
    yanking the page ("no way to fix except restart"). Now any NEW
    touchstart while the stream is silent >2.5s (and no active touch near
    the gesture's last position — a merely-PARKED finger is protected)
    ends the gesture (drags COMMIT, pointercancel policy) + traces
    "[jrndrag] zombie drag healed…". press-drag.test.js ×5 (RED-proven ×3
    vs the pre-fix engine). Live-proven: wedged drag ate touchmoves → one
    touch later ghost gone + scroll free + trace emitted.
LONG-ENTRY DRAG — edge autoscroll rebuilt: zones derive from the
    scroller's visible rect (the old window-edge zones buried ~60px of the
    top zone under the nav), zone ~150px, speed = depth² curve × time ramp
    (holding accelerates, cap ~48px/tick ≈ 3000px/s); loop also bails when
    the engine reports the drag ended. Live-proven: full ~3800px
    top→bottom drag in ONE gesture (1225px scrolled in the first 1.5s),
    commit persisted.
(9) INSERT NEVER SPLITS — insertAtCursor → insertBlockBelow: the new block
    lands BELOW the caret's block, the paragraph stays whole, the caret is
    restored in place (pendingFocusRef {id, caret}); if the insert becomes
    last, ONE trailing empty paragraph is appended; the no-caret-context
    fallback (reuse/append exactly one blank p) is preserved. +3 editor
    tests (RED-proven ×2 vs the split behavior).
(8) EDIT AFFORDANCE DECIDED — the viewer's pen FAB is THE edit affordance;
    the ⋯ menu is Pin / Delete only (JournalCardMenu hideEdit; the hub
    menu keeps Edit — no on-screen pen there). Plainness: the editor shows
    the entry date under the title (same eyebrow as the viewer); TEXT
    blocks' delete × is a whisper (0.22) until the block is focused (0.8,
    :focus-within) — media/card blocks keep the always-visible ×. Menu
    test updated to pin Edit's absence.
Gates: 2583 vitest / 145 files (+11: press-drag ×5, scroll-memory ×3,
editor insert ×3), lint --max-warnings 0 (441 globals unchanged),
typecheck, build, smoke:ci PASS. Preview-proven at 375px on app-alt
(another session held 8090): 0 console errors, no horizontal overflow.
───────────────────────────────────────────────────────────────────────────
Owner items:
  (8) "Redesign in-journal entry menu and navigation to look better and be
      more intuitive and plain; decide between ⋯ > edit or bottom-right pen
      icon to edit; all items draggable with live rearrange around the
      dragged item."
  (9) "Inserting an item should never split other items — always insert
      beneath the current one."

RECON: The viewer TODAY has BOTH a pen FAB (jrn-fab is-edit) and a ⋯ menu
(JournalCardMenu hideOpen) — the "decide" is real: pick ONE primary edit
affordance. Drag-to-reorder with live FLIP rearrange LANDED 2026-07-03
(b048e07) for ALL block types in the editor — verify it matches the owner's
bar on-device; polish if not. insertAtCursor (JournalEditorScreen.jsx:572)
DOES split a text block at the caret (head/tail slices) — that's item 9's
target.

PLAN:
- (9) insertAtCursor → insert AFTER the current block (whole-block
  granularity; caret position no longer splits text). Keep the "no caret
  context" fallback (insert before trailing blank p). Update reorder tests.
- (8) Decision: KEEP the pen FAB as the one edit affordance (Google-Keep
  idiom, discoverable, thumb-reachable); ⋯ menu keeps Pin/Delete(+share
  later) and DROPS Edit (redundant). Sweep viewer+editor chrome for
  plainness: labels under ambiguous icons, spacing, hierarchy.
- Re-verify drag UX in preview (grips, live rearrange, autoscroll).

───────────────────────────────────────────────────────────────────────────
SESSION 4 — Text size slider, text-only scaling (item 2)
───────────────────────────────────────────────────────────────────────────
STATUS: DONE 2026-07-19 (commit 321f5c0). Slider 80-160% step 5% + live %
readout + Reset + in-row preview line; same settings.fontScale key, numeric
clamp [0.8,1.6] in use-settings + the boot script (pre-mount, no FOUC).
Text-only scaling via the px chrome-pin block at the END of app.css (top
nav, back glyph, arrows, dot, tabs badge, sel-toolbar, ann-chip, mode
pill). Live-proven: nav 67.2px identical at 0.8/1.0/1.6; verse text scales
exactly; icons never wrap (pinned, per the owner-question answer below).

Owner: "Adjustable font size slider — shrink OR expand. Must NOT expand
icons; icon scaling would be a separate feature. Wrap icons if they'd spill?"

RECON: WL1 today = Settings SelectField with 4 steps (1.0/1.15/1.3/1.5) →
`html{font-size:calc(100% * var(--font-scale))}` → EVERYTHING rem/em-based
scales, including nav chrome + glyph icons. index.html boot script applies
the persisted value pre-mount (no FOUC) — keep that contract.

PLAN:
- Slider (range ~0.8–1.6, step 0.05) with a live preview line + reset-to-
  standard; replaces the 4-step selector. Persist same settings.fontScale key
  (numeric string; old values remain valid).
- TEXT-ONLY scaling: keep the root multiplier (it's what makes every reading
  surface scale, incl. injected journal/highlights CSS) but PIN app chrome:
  top-nav, sticky nav, toolbars, sheets' control rows, FABs, badges, icon
  buttons get px-locked font-size/dimensions (audit pass; the svg icons are
  already width/height px — the GLYPH icons (✕ ⋯ ‹ ›) + button labels are the
  scaling leak). Acceptance: at 0.8 and 1.6, chrome is pixel-identical;
  reading text, journal text, note bodies, search snippets scale.
- ANSWER to owner's wrap question (also said in session summary): icons
  should NOT wrap — with icons pinned they can't spill from text scaling;
  if a future icon-size feature lands, cap it / collapse overflow into ⋯
  rather than wrapping (wrapped toolbars destroy muscle memory).

───────────────────────────────────────────────────────────────────────────
SESSION 5 — Polish batch: selection restriction + spacing audit (items 4 + 5)
───────────────────────────────────────────────────────────────────────────
STATUS: DONE 2026-07-20. (4) chrome unselectable: `button` + top-nav +
sheet chrome + chain-nav cards + back-pill get user-select/-webkit-user-
select/touch-callout none (buttons never contain user prose here; inputs/
textareas opt back in; programmatic Ranges — the annotation engine, the
pickers, smoke's letterAnn/wtlbAnn round-trips — ignore user-select, all
verified green). Reading text measures userSelect:auto. (5) Search group
chevron: 24x24 rotating .srch-group-chevron replaces the 0.55rem glyph;
Settings danger row: head wraps so "Clear All My Data" drops to its own
line (the info-btn can never sit flush against it) + a 12px hard minimum
gap on DataActionRow buttons.

Owner items:
  (4) "Can't highlight anything (tabs number) besides text." → interpreted:
      UI chrome (e.g. the tabs-count button in the top nav) is long-press
      SELECTABLE today; only reading/user text should be selectable.
  (5) "Proper button spacing — ⓘ info icon right next to delete button on
      'delete my data' in Settings; sweep all screens. Search's expand
      chevrons (volume/album groups) are tiny and not apparent."

PLAN:
- (4) user-select:none on app chrome (nav bars, buttons, badges, sheet
  chrome), explicit user-select:text opt-in on reading containers, journal
  text, note bodies, inputs/textareas. Careful: SelectionToolbar's long-press
  flow must keep working (reading containers stay selectable); Android
  WebView needs -webkit- prefixes; the tap→chip native bridge unaffected.
- (5) Settings danger zone: separate the ⓘ from "Clear All My Data" (own
  row/gap). SrchGroup header chevron: larger (≥24px), higher-contrast, with
  a real rotation affordance. Then a measured sweep of icon-button hit areas
  + destructive-adjacent gaps (the 2026-07-02 passes fixed many; re-walk
  with fresh eyes on Settings + Search + Library headers).

───────────────────────────────────────────────────────────────────────────
OWNER FOLLOW-UPS LANDED SAME DAY (outside the 5 sessions)
───────────────────────────────────────────────────────────────────────────
(A) TAB THUMBNAILS vs THEME SWITCH — owner: mixed dark/light cards after a
theme change. v1 (invert-filter + theme metadata) shipped first; owner
device-tested it: "pretty convincing" BUT hero hues drifted blue (the
filter is an approximation) and pre-update legacy cards didn't participate
(his Library card stayed dark). He proposed remembering BOTH theme states —
implemented as v2 the same day: native capture can only shoot the on-screen
theme, but html2canvas renders from a DOM CLONE, so the new
PlatformBridge.takeThemedScreenshot forces the opposite theme class on the
clone (onclone) and renders the other theme invisibly (works on Android
too — html2canvas is in the same JS bundle). Entries are variant maps
{dark?, light?, unknown?}: primary capture fills the current slot; a
~900ms-deferred themed render fills the other (seq-guarded, overview-
suppressed); TabsOverview picks the matching variant → theme flips are
INSTANT lookups with true pixels; the invert filter remains only as a
transitional fallback; classifyThumbTheme (luminance probe) upgrades
legacy `unknown` rows so old cards participate too; garden = one capture
fills both slots. Live-proven: app stayed dark → row {dark 82KB, light
72KB}, luma 2 vs 239; overview served each theme's variant unfiltered.
(A2, owner re-test) TABS-SCREEN HOME BUTTON DEAD + PC BLANK THUMBNAILS —
(1) the overview's HomeBtn called __goHome, which changes the screen UNDER
the overlay (looked like a no-op on both platforms). HomeBtn gained a
`beforeGo` prop; the overview passes its own dismiss. (2) html2canvas
occasionally yields a ZERO-SIZED canvas (boot races); its toDataURL is
"data:," — truthy, so it was STORED and painted that tab card permanently
blank (background tabs never recapture → the owner's PC wall of blank
squares). Fixed at three layers: webTakeScreenshot rejects tiny canvases +
sub-1000-char encodes ('' = failure, next capture retries); mergeVariant
floors incoming URLs; the load-on-mount SCRUBS stored degenerate variants
(drops the field, deletes emptied rows → placeholder until the next good
capture heals it). Live-proven: seeded {dark:'data:,',light:'data:,'} row
deleted at boot; overview Home tap closes the overlay + lands Home.
(A3, owner re-test: PC STILL blank; Android fine) — local repro is healthy
(reading-screen captures 110KB primary + 102KB themed, ~750ms each), so the
PC failure is ENVIRONMENTAL (prime suspect: an extension injecting body
nodes that make html2canvas throw, or canvas-read blocking; extensions run
in installed PWAs too — matches PWA+site failing while Android's native
path works). Hardened 3 ways: (1) capture targets #root, not document.body
— extension junk is body-level, so it can no longer break the render (also
excludes body-portaled sheets from cards); (2) a 16×16 uniformity probe
rejects blank renders (luma range <6 → '' — real screens carry text
contrast; also catches canvas-blocker white-outs at the store level);
(3) every web-capture failure now traces via _thumbFail → console.warn
"[thumb] …" + DiagnosticLog('thumb'). If the owner's PC still shows
placeholders after this build: F12 → Console → send the [thumb] lines —
they name the exact cause.
(A4, owner re-test: PC thumbnails NOW WORK but "don't fill or fit the
tabs") — root cause: the capture was the whole window; on a wide PC the
app is a centered 760px .screen-layout column with empty flanks, so cards
showed a thin strip floating in blackness. Fixed: web captures now target
.screen-layout (the app's true viewport; #root/body fallbacks keep the
extension hardening), and --card-ar derives from the COLUMN width (via
shared updateCardAr(), re-run at capture time — the mount effect can fire
while a lazy-corpus placeholder has no column yet). Phones: column ==
window → Android/mobile byte-identical (owner constraint). Live-proven at
1280×720: capture 760×720, --card-ar '760 / 720', card 231×219, img fills
card edge-to-edge.
(B) EXCERPT-PICKER "can't save until I scroll" — the picker only captured
selection on mouseup/touchend over the body, but Android's long-press
selection delivers its touchend NON-BUBBLING (same WebView behavior as the
tab-drag lockup) and HANDLE drags fire no page touch events at all — a
later scroll's touchend was what finally ran the capture. Fixed: document
'selectionchange' listener (150ms debounce) drives the state commit; touch/
mouse handlers stay as fast path. Live-proven: programmatic Selection.addRange
(fires ONLY selectionchange) flipped the footer to "Insert this excerpt".
PREVIEW GOTCHA: hidden-tab setTimeout clamps to ~1s — sample AFTER generous
settles or the 150ms debounce looks broken.

(C) ZOOM STUCK + TAB-DRAG REDESIGN (owner-reported 2026-07-13) —
(1) STUCK ZOOM: the APK disables WebView zoom, but the viewport meta had no
maximum-scale/user-scalable — the INSTALLED PWA could pinch/auto-zoom
(small-input focus) and the app's own touch handlers (pager preventDefault)
then fought the zoom-OUT pinch, snapping it back until an app restart.
Fixed: `maximum-scale=1.0, user-scalable=no` (the app is deliberately
non-zoomable; text size has its own Settings control).
(2) TAB DRAG REBUILT (v2) after two patch rounds still left device
failures ("works once then never until restart" + "the real card moves
again after the ghost lands"): now a POINTER-EVENTS state machine — one
gesture object per pointerdown, destroyed by a single idempotent
endGesture() from every exit path; a new pointerdown FORCE-resets anything
a previous gesture leaked (no wedged state can refuse grabs); pointercancel
(the browser claiming the stream — the silent device killer) explicitly
commits at the current slot + traces "[tabdrag] …" (console + Diagnostic-
Log); document-CAPTURE listeners kept; the touchmove preventDefault scroll
suppressor kept (pointer events can't cancel scrolling). SEAMLESS DROP:
the reorder + sibling-transform clear now commit in the SAME task at
release (one paint of the final order) while the ghost glides above the
inline-hidden destination card and swaps 1:1 at landing — the old path
cleared transforms → painted the OLD arrangement → reordered 240ms later
(the visible double-move). Also: the thumbnail <img> was natively
DRAGGABLE — a long-press/drag landing on it could start the browser's
image-drag and kill the touch stream (a "worked once, then thumbnails
appeared, then never again" candidate); draggable=false + onDragStart
preventDefault + -webkit-user-drag:none + pointer-events:none on the img.
TabsOverview.drag.test.jsx rewritten: ×10 pointer-event pins (synchronous
commit, hidden-dest landing swap, SECOND-drag-after-success, non-bubbling
pointerup, pointercancel commit, multi-pointer identity, non-primary
ignore, drift cancel, onReorder-throw recovery, unmount leak-free).
Live-proven: 3 consecutive drags engaged + committed, ghost glide with the
order already final beneath, 0 leaks, 0 console errors.
(C2, owner confirmed "they feel great" → MIGRATION COMPLETE) — the
lifecycle extracted into utils/press-drag.js (createPressDrag: gesture
object, capture listeners, scroll suppressor, hold/glow/drift, pointer-
cancel commit+trace, force-reset, landing glide, click suppression; hosts
own geometry via onEngage/onDragMove/onCommit/onAbortDrag + g.data).
TabsOverview rebased (10 pins unchanged = equivalence proof); HomeScreen +
LibraryScreen + JournalEditorScreen (holdMs:0 grip) ported — all commits
now synchronous at release with the seamless hidden-dest drop (ID-keyed
tiles reveal the GRABBED node; index-keyed tabs reveal cardRefs[to]); the
journal's 220ms parked-commit window is gone (pagehide-flush guarantee now
unconditional). Traces: [homedrag]/[libdrag]/[jrndrag]. Journal reorder
suite rewritten to pointer events (×9). Live-proven on all three ported
surfaces (reorder + persist + second drag + restore each), 0 console
errors. Gates: 2572 vitest / 144 files, lint(441), typecheck, smoke PASS.

───────────────────────────────────────────────────────────────────────────
STATUS LOG
───────────────────────────────────────────────────────────────────────────
2026-07-12  File created; recon of all 9 items done (surfaces verified in
            source). Session 1 executed this session — see top.
