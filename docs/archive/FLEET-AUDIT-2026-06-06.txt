================================================================================
FLEET-AUDIT-2026-06-06.txt
VOTReader — 16-agent Opus code audit + remediation tracker
================================================================================

--------------------------------------------------------------------------------
SESSION HANDOFF — 2026-06-07 (next agent: START HERE)
--------------------------------------------------------------------------------
STATE (after SESSION 2, 2026-06-07b — the TAIL IS NOW CLOSED). The whole audit is
DONE or ADJUDICATED-with-reason. Session 1 closed every P0+P1+P2 + the valuable P3
(25 DONE + 3 ADJUD). Session 2 then swept the ~49-item tail: 15 MORE DONE (BLD1,
BAK3, BAK5, SEC2, ANN4, ANN5, TEST3, TEST5, CORP6, BLD2, NTV3, PERF4, SRCH4, ERR3,
NTV1) + ~31 ADJUDICATED with re-derived reasons (each re-checked against current source by a
per-cluster agent, then verified by me) + 4 found ALREADY-DONE/SUBSUMED (SW3 by SW1,
NTV2, TEST2, plus PERF5/NAV3/STOR2 from session 1). See SECTION 1 for the per-item
status and the 2026-06-07b PROGRESS LOG entry (bottom) for the disposition + reason
of every item. Verify: vitest + lint + typecheck + gradle :app:testDebugUnitTest all
green; PERF4 preview-verified on Psalm 119; backup-integrity + bridge changes gated.

NOTHING is left for autonomous work — the audit is FULLY closed. After the owner
cleared them (2026-06-07b), ERR3 + NTV1 landed too (commits 0b5c64e + e8d6b50):
  - ERR3 DONE — AppShellOverlays + AppShellSheets each wrap their own return in
    <ErrorBoundary fallback={null}>, so a render crash in any sheet/overlay vanishes
    + logs instead of nuking the whole app via the root boundary. NO app.jsx change
    (still 798/800), no reorder — the cleaner fix than the <AppShellChrome> extraction
    originally sketched. +3 ErrorBoundary tests; smoke:ci green.
  - NTV1 DONE — restoreAudioModeIfActive() (guarded on MODE_IN_COMMUNICATION) called
    from onRenderProcessGone() + onDestroy(); compiles + Kotlin suite green.
  - PERF1 ADJUDICATED (won't-do): its cold-boot win matters mainly on SLOW devices
    the owner doesn't have, the app isn't render-intensive, and it's the riskiest
    remaining change (the synchronous cold-boot path) — net-negative to restructure
    for a marginal gain against the owner's stated low perf concern (owner call,
    2026-06-07b). Revisit only if a budget-device audience or a measured cold-boot
    problem ever appears; the plan (lazy-load books-restored.js + matthew-plain.js
    via __makeLazyLoader, typeof-guarded consumers, SW precache, CORPUS_VERSION bump)
    is recorded should that change.

  REMAINING is optional + owner-side + NON-blocking: device CONFIRMATIONS for the two
  device-touching fixes already shipped — NTV3 (Garden cache empty after Clear-All) +
  NTV1 (routing returns to speaker after a mid-record crash) — plus the standing manual
  device walks (N2.x audio-tuning, U-series import/Garden) in tools/n1-smoke-walk.md.
  BLD2's widened deploy gate already ran GREEN in Actions (confirmed).
  GOTCHA confirmed this session: the pre-commit `npm run build` builds from the WORKING
  TREE, so commit ONE logical change at a time (uncommitted source from a second change
  bakes into the first commit's bundles → an inconsistent commit). Also: don't use
  backticks in a bash `-m` commit message (command substitution eats the word).

GOTCHAS (load-bearing — learned the hard way this session):
 1. AUDIT FINDINGS CAN BE WRONG. Re-derive every finding against CURRENT source
    before fixing — the agent reports' line numbers drift, and two findings were
    outright false-positives (STOR2: the real "Clear All" deletes whole IDB DBs via
    _deleteIdbDatabase, NOT a merge path; NAV3: the __goSearch no-deps re-bind is
    the INTENTIONAL fresh-closure bridge). Prove it, don't trust it.
 2. app.jsx is 798/800 (the canary) — effectively FULL. Any app.jsx-touching change
    must extract a hook first. PowerShell `Measure-Object -Line` UNDERCOUNTS; trust
    `wc -l` / `npm run check:app-size`.
 3. CORPUS_VERSION is now c9. Bumping it (any books/matthew/VOT corpus OR
    bible-studies.js / bible-<code>.js edit) REQUIRES also bumping
    CORPUS_CONTENT_VERSION in app/src/main/assets/search.js to match — the
    corpus-version gate fails CLOSED if they diverge (SRCH1).
 4. KEYED RE-RENDER pattern: getVersionForKey/_bumpKey live on the base CachedStore.
    To stop a whole-page re-render on one edit, call _bumpKey(key) on the store's
    mutations + subscribe getVersionForKey(key) (done for AnnotationStore + NoteStore
    [APP1]). Link/Bookmark CAN'T use it — their icons match by KEY PREFIX, so exact-
    key versioning is unsound (that's why PERF5 was adjudicated).
 5. VERIFY: vitest + `npm run lint` (--max-warnings 0) + `npm run typecheck`; the
    pre-commit then re-runs all gates on commit (it rebuilds bundles + re-stages, so
    you don't hand-edit dist/). Render check: `npm run smoke:ci`. LIVE preview: do a
    clean-slate first (unregister SWs + delete caches + reload); to test SEARCH also
    `indexedDB.deleteDatabase('vot-search-cache')`. Run npm via git-bash (PS blocks
    npm.ps1). Commits go DIRECT to main; push after each logical unit.
 6. NTV1 (Kotlin audio-mode restore) + BLD2 (deploy-gated-on-CI) CANNOT be runtime-
    verified from this environment — do them where you can watch the emulator/Actions.

NEXT (my recommendation). CORP2 + NAV2 are now DONE. Remaining real fixes: APP2
(scroll useLayoutEffect — render-path, mind the existing rAF in use-scroll-memory
Effect 3), SRCH2 (search.js stop-word skip — short-circuit an all-stopword query),
TEST2 (React 18.2-runtime vs 19-test skew — mostly documentation). Then the P3
tail (SRCH3-6, JRNL1-3, NTV2-4, etc.). NTV1 + BLD2 want a device/CI pass.

--------------------------------------------------------------------------------
HANDOFF (READ FIRST)
--------------------------------------------------------------------------------
WHAT THIS IS. On 2026-06-06 a fleet of 16 independent Opus-4.8 agents
adversarially audited VOTReader, CODE ONLY (no UI/preview/device walks, no
accessibility, no splash/visual). Each agent owned one subsystem, verified every
finding against live source (several reproduced bugs through production code or
ran the suite), and returned a 1-10 rating + ranked findings. This file is the
canonical tracker for closing them. The full per-agent reports (more LOW/INFO
detail than is transcribed here) are in the session transcript.

OVERALL: 8.2/10. An exceptionally well-engineered personal app — visibly the
product of prior audit passes. The data-lifeline subsystems (journal durability,
security, backup, storage) are excellent. The gap to 9+ is a small, specific,
mostly-mapped set of fixes, several found by TWO agents from different angles.

SUBSYSTEM SCORECARD
  Journal durability .......... 9     App() / hooks / state ....... 8.5
  Security (local-only) ....... 9     Test-suite quality .......... 8.5
  Backup / export-import ...... 8.5   Annotation engine ........... 8
  Build / tooling / CI ........ 8.5   Storage core / cross-tab .... 8
  Kotlin / Android native ..... 8.5   Scripture resolution ........ 8
  Data integrity / corpus ..... 8.5   Error handling / resilience . 8
                                       Performance / memory ........ 7.5
                                       Service worker / PWA ........ 7
                                       Navigation / back-stack ..... 7

METHOD (per item): verify current state against source (agents can be stale/
wrong — re-derive) -> reproduce -> fix -> PROVE (vitest and/or preview clean-
slate, build, smoke:ci) -> gate green (lint --max-warnings 0, typecheck,
coverage floor, check_balance, validate-schemas, corpus-version, csp-hash,
app.jsx <=800 canary) -> commit -> push -> tick this file's PROGRESS LOG.

OUT OF SCOPE (settled owner policy — do NOT add): auth/login, AI/LLM/API-keys,
cloud sync/server, telemetry/crash-reporting services, multi-user, ACCESSIBILITY
(separately in-flight), UI/visual/splash redesign, Play Store, old-browser
polyfills (WebView floor is chrome108 / evergreen).

LOAD-BEARING GOTCHAS
  - Preview clean-slate to load fresh bundles past the SW cache (unregister SWs +
    delete caches + reload). To verify SEARCH you must ALSO
    indexedDB.deleteDatabase('vot-search-cache').
  - jsdom has no navigator.locks -> vitest.setup.js ships a mutex shim. It is
    faithful; it does NOT mask STOR1 (that's a merge-ALGORITHM flaw).
  - A new window-globalized export needs `npm run lint:globals` + re-stage
    (pre-commit auto-regenerates now, but confirm).
  - CORPUS_VERSION manual bump on any books/matthew/VOT *and* (newly) on
    bible-studies.js if it enters the SW corpus cache (SW1).
  - app.jsx is under an ENFORCED <=800-line canary. Prefer pushing new state/
    effects into hooks over bloating App().
  - esbuild --target=chrome108 on build:b/c/d/e/css + corpus minify (Permanent
    Rule 6). Floor is a RUNTIME-API contract, not just syntax.

--------------------------------------------------------------------------------
SECTION 1 — PRIORITY INDEX
--------------------------------------------------------------------------------
P0  real correctness, user-facing
  ERR1  [HIGH] Bible-Studies lazy-load failure = permanent silent dead-end       DONE
  SW1   [HIGH] Studies + 7 alt-translations are network-only (broken offline)    DONE
  ERR2  [LOW]  Alt-translation load failure swallowed, no diagnostic trace       DONE
  NAV1  [HIGH] Android hardware-back consults wrong modal registry (~10 surfaces)DONE
  STOR1 [HIGH] Cross-tab: deleting last annotation on an anchor resurrects it    DONE
  BAK1  [MED]  Android truncated-import overwrites stores then reports "failed"   DONE

P1  high value, low-moderate risk
  PERF1 [HIGH] ~506 KB deferrable corpus on the synchronous cold-boot path       ADJUD
  PERF2 [HIGH] Journal media object-URL cache unbounded per session (OOM)        DONE
  APP1  [MED]  NoteStore global-bump re-renders whole page (Theme B)             DONE
  PERF5 [LOW]  Link/Bookmark icons re-render on any mutation (Theme B)           ADJUD
  NTV1  [MED]  Audio MODE_IN_COMMUNICATION never restored on teardown/kill       DONE
  SRCH1 [MED]  Search index cache invisible to CORPUS_VERSION (stale forever)    DONE
  SCR1  [L-M]  Translation tag with no shipped bundle silently renders NKJV      DONE
  STOR2 [MED]  "Clear All Personal Data" leaves residue under sibling-tab write  ADJUD
  SEC1  [LOW]  Runtime-string toasts route to an innerHTML sink                  DONE

P2  gates + correctness tail
  BLD1  [MED]  No gate proves minify preserves corpus VALUES                     DONE
  BLD2  [MED]  Web deploy publishes independently of CI (thin gate subset)       DONE
  CORP1 [MED]  Schema validator does not detect duplicate id slugs              DONE
  CORP2 [MED]  Alt-translation Bibles get no completeness cross-check            DONE
  ANN1  [MED]  Multi-verse note paints N icons (React) vs 1 (DOM)                DONE
  ANN2  [MED]  "Remove highlight" silently deletes overlapping NOTES             DONE
  NAV2  [MED]  pendingHighlight navHandoff slot leaks (peek + conditional clear) DONE
  NAV3  [MED]  __goSearch bridge effect: no deps, unconditional null on cleanup  ADJUD
  APP2  [L-M]  Scroll restore in passive effect, not useLayoutEffect (flash)     DONE
  PERF3 [MED]  Reading screens re-render full verse list on every scroll-flush   ADJUD
  PERF4 [MED]  content-visibility:auto unused (safe win at chrome108 floor)      DONE
  TEST1 [MED]  tabs.js + highlight.jsx at 0% coverage (load-bearing pure utils)  DONE
  TEST2 [MED]  React 18.2-runtime vs 19.2-test skew unmitigated/unexercised      ADJUD
  SRCH2 [L-M]  Pure-stopword/<=4-word queries skip trimming -> full-corpus scan  DONE
  BAK2  [L-M]  Missing manifest media `size`: rejected on web, accepted on Android DONE
  BLD3  [L-M]  Pre-commit fails open when staging only a dist bundle / app.jsx   ADJUD
  CORP3 [L-M]  check_balance smart-quote heuristic near-vacuous (esprima saves)  ADJUD

P3  small wins / hardening / adjudicate
  ANN3 [LOW] applyDOMHighlights O(n^2) offset re-walk per sub-range (adjudicate) ADJUD
  ANN4 [LOW] applyNoteIcons fragments text nodes (no normalize on icon path)     DONE
  ANN5 [LOW] No test for elementsFromPoint tap-routing winner                    DONE
  SRCH3 [LOW] Two volume-ID registries hand-synced, no equality assertion test   DONE
  SRCH4 [LOW] Snippet ignores synonym/cross-translation matches (no highlight)   DONE
  SRCH5 [LOW] Engine search() has no min-length floor (only UI gates)            ADJUD
  SRCH6 [INFO] Dead code: STOP_WORDS, 'chapter-title'/'heading' kinds, phoneticKey ADJUD
  STOR3 [INFO] _base refreshed only in _saveMerged (benign today; future trap)   ADJUD
  STOR4 [LOW] _saveMerged structuredClone of largest store on every save         ADJUD
  STOR5 [LOW] Lock-unavailable merge fallback is silent (no diagnostic)          DONE
  BAK3 [LOW] `counts` integrity block written but never verified on import       DONE
  BAK4 [LOW] Legacy import cap asymmetric: 50 MB Android vs 300 MB web           DONE
  BAK5 [LOW] Web FS-Access export leaves partial file on mid-stream write fail   DONE
  BAK6 [INFO] streamedIds plain-object key-set unreliable for id "__proto__"     DONE
  JRNL1 [LOW] Rapid double media-insert relies on debounce for the 2nd block     ADJUD
  JRNL2 [LOW] Recording save() in error stage arms a no-op pendingSaveRef        ADJUD
  JRNL3 [LOW] recordDeletion can drift totalEntries vs real count (cosmetic)     ADJUD
  JRNL4 [INFO] Single-slot draft key safe only via ts>=updated guard (document)  ADJUD
  NAV4 [LOW] Swipe-nav fires on any horizontal touch (no target/intent filter)   DONE
  NAV5 [LOW] Browser-back after tab switch backs the screen, not the tab         ADJUD
  NAV6 [INFO] Synchronous double-back reads stale refs (unreachable normally)    ADJUD
  SCR2 [LOW] Fallback mis-resolves comma-list / multi-book refs (dict masks it)  ADJUD
  SCR3 [LOW] Cross-chapter ranges fail to parse (no such refs in corpus today)   ADJUD
  SCR4 [INFO] Book-only refs return null (by design; documented)                 ADJUD
  SW2 [LOW] corpusFirst cache.put is fire-and-forget (may die before commit)     DONE
  SW3 [LOW] HTTP-cache staleness on network-only data files (subsumed by SW1)    DONE
  SW4 [INFO] CORE_CACHE eviction has no self-heal (persist() mitigates)          ADJUD
  APP3 [LOW] useReadingDwell visibility-resume reads stale dwellMs               ADJUD
  APP4 [INFO] Lazy-corpus subscription depends on index.html script order        ADJUD
  APP5 [LOW] vot-state full-serialize write amplification on every scroll-stop   ADJUD
  APP6 [INFO] 800-line canary forces two cohesion seams (documented)            ADJUD
  NTV2 [LOW] Aborted v3 export can orphan a partial file the abort can't delete  DONE
  NTV3 [LOW] GardenImageCache.clear() is dead code; Settings wipe unwired        DONE
  NTV4 [LOW] Recorder pause()/resume() report success with no active recording   ADJUD
  NTV5 [INFO] getZoomScale() transient during in-flight screenshot (benign)      ADJUD
  NTV6 [INFO] audioSystemService nullability unreachable (benign)                ADJUD
  BLD4 [LOW] esbuild pinned by caret; minifier change would silently re-emit     ADJUD
  BLD5 [INFO] CORPUS_VERSION correctness is transitive through "CI rebuilds"      ADJUD
  SEC2 [LOW] JSON.parse exception message surfaced to user (folded into SEC1)    DONE
  CORP4 [LOW] esprima 4.0.1 can't parse ?./?? -> false-positive if added to data ADJUD
  CORP5 [LOW] No num-sequence validation (gaps/dups/order)                       ADJUD
  CORP6 [INFO] audit-footnotes.js is strong but not wired into CI                DONE
  TEST3 [LOW] Real-time setTimeout(r,5) monotonic-ts assertions (flake)          DONE
  TEST4 [LOW] Device-only compressImage error branches unverified                ADJUD
  TEST5 [LOW] No-navigator.locks merge fallback untested                         DONE
  ERR3 [LOW] AppShell chrome renders outside the inner ErrorBoundary             DONE

--------------------------------------------------------------------------------
SECTION 2 — THEMES (cross-agent convergences = highest signal)
--------------------------------------------------------------------------------
THEME A — Secondary-corpus cache/offline/recovery gap (5 agents converge).
  The main 3 corpus bundles (Bible/Matthew/VOT) are bulletproof, but Bible
  Studies + the 7 alt-translations load through the OLDER translations.js path
  that predates the __makeLazyLoader retry/diagnostics fix and was never
  migrated. Consequences stack: SW1 (broken offline), ERR1 (silent permanent
  dead-end), ERR2 (no diagnostic), SRCH1 (stale search cache), SCR1 (silent NKJV
  under a foreign cite). ONE focused session on translations.js + the SW corpus
  list + the search-cache signature + a translation allowlist closes a HIGH
  offline bug, a HIGH dead-end, and three smaller items together.

THEME B — Per-key reactivity only half-applied (3 agents converge).
  F1/F2 gave AnnotationStore getVersionForKey so one highlight edit re-renders
  one verse. NoteStore / LinkStore / BookmarkStore still only _bump() globally,
  so one note/link/bookmark edit re-renders EVERY verse on the page (176 on
  Psalm 119). APP1 + PERF5. Mechanical extension of a pattern already built.

THEME C — Cross-tab / multi-surface edge cases.
  Right in the common case, wrong on a 2nd tab / open sheet / emptied bucket:
  STOR1 (annotation resurrection), STOR2 (Clear-All residue), NAV1 (Android
  back split-brain).

THEME D — Android native + backup durability tail.
  NTV1 (audio mode restore), BAK1 (partial-import store overwrite), NTV2
  (orphaned partial export), BAK2 (cross-platform size asymmetry).

THEME E — Gate completeness.
  BLD1 (minify value fidelity), BLD2 (deploy gated on CI), CORP1 (dup-id),
  CORP2 (translation completeness), BLD3/BLD4.

--------------------------------------------------------------------------------
SECTION 3 — DETAIL (fix direction per item; * = part of a Theme)
--------------------------------------------------------------------------------
ERR1* translations.js:42-54 + app.jsx:256-265 + StudiesHome.jsx:42-44. onerror
  resolve()s AND caches the resolved promise -> studies stay [], "coming soon"
  forever, never retries, logs nothing. FIX: on error null the cached promise +
  signal failure; StudiesHome shows "Try again"; DiagnosticLog.error. Lowest-risk
  shape: mirror the __makeLazyLoader retry/diagnostics contract.
SW1*  service-worker.js:24-105,188-194 + translations.js:33,47. bible-studies.js
  (4.4MB) + 7 bible-*.js (~32MB) are network-only; coreFirst never cache.puts a
  miss. FIX: add bible-studies.js to CORPUS_PRECACHE (bump CORPUS_VERSION); give
  src/data/bible-*.js a corpusFirst cache-on-use branch; surface load failure.
ERR2* translations.js:36. loadTranslation onerror swallows silently. FIX:
  DiagnosticLog.warn('translation','failed '+code) before the NKJV fallback.
SRCH1* search.js:148-180 dataSignature is structural (lengths) only; never reads
  CORPUS_VERSION -> a reworded verse serves the old index forever. FIX: fold
  CORPUS_VERSION (or a content hash) into dataSignature.
SCR1*  scripture-resolution.js:362-376 + translations.js:33. Tags CJB/GNT/ESV/
  NLT/NAS/CEB (no bundle) fall through to NKJV silently if the dict key ever
  misses. FIX: allowlist loadTranslation to the 7 real bundles; on fallback,
  strip the unsupported tag from the displayed cite.

NAV1  use-android-back.js:165,312. handleAndroidBack checks only __closeSheet;
  Escape checks only modalRegistry and returns early on Android. Disjoint sets ->
  back over NoteSheet/SelectionToolbar/BookmarkPopover/Garden-warning/Welcome
  dismisses AND navigates underneath. FIX: handleAndroidBack consults
  modalRegistry FIRST (peek().dismiss; return "true"); fold __closeSheet sheets
  into the registry; collapse both platforms onto one dispatcher.
STOR1 store-merge.js:165-179 (mergeMapByKey) + annotation-store.js:119,134.
  Empty-bucket delete does `delete data[key]`; outer merge then reads
  key-absent-but-base-present-and-theirs-edited as our-delete-loses ->
  resurrects sibling bucket incl. our just-deleted segment. FIX: when the value
  merge is a sub-collection merge, coerce absent ours/theirs to empty and ALWAYS
  run the nested merge vs base[k]; OR AnnotationStore set data[key]=[] not delete.
STOR2 journal-store.js:519-525 clear() routes through the 3-way merge -> a
  sibling-added record survives an explicit wipe. FIX: a _saveAuthoritative()
  that bypasses read-merge and refreshes _base for whole-store overwrites.
BAK1  backup.js:629-665 + SettingsScreen.jsx:807-820. applyV3 applies+saves
  stores BEFORE streaming media; on Android a truncated frame throws mid-apply ->
  stores durably overwritten, media not, UI shows "Import failed", no reload.
  (Web is immune: readContainer pre-validates all frames.) FIX: on Android drain+
  integrity-check all frames before applyV3 (mirror web), OR apply media first,
  OR force a reload on the throw so durable writes + UI agree.

PERF1 build.py:69-70 + index.html:132. bundle-a is 81% corpus: books-restored.js
  (277KB, backs an OFF-by-default feature) + matthew-plain.js (229KB) are
  reachable only behind already-lazy walls. FIX: move both behind __makeLazyLoader
  (replace SettingsScreen's matthew-plain .chapters.length read with literal 28).
PERF2 journal-media-store.js:259-270,152. _urlCache[id]=createObjectURL never
  evicts in-session -> ~150-200 photos pin 200-400MB. FIX: LRU (mirror garden
  _gardenLru) ~20-30 cap, revokeObjectURL on the victim.
APP1* annotation-engine.jsx:192-195. HighlightableText subscribes NoteStore
  .getVersion() (global). FIX: add NoteStore _bumpKey(hlKey)+getVersionForKey;
  subscribe keyed (note class depends only on the verse's own group).
PERF5* LinkIcon.jsx:8-15 + BookmarkIcon.jsx:9-12 subscribe global versions,
  unmemoized, getForKey is O(L)/O(B). FIX: getVersionForKey on Link/Bookmark
  stores + React.memo the icons keyed on hlKey.
NTV1  MainViewModel.kt:73-79 / MainActivity.kt:902-928. startAudioSession sets
  MODE_IN_COMMUNICATION; only the JS unmount path restores it -> mid-recording
  kill leaves the whole device in earpiece routing. FIX: restore audioManager.mode
  in onCleared (+ onPause-while-recording + onRenderProcessGone).

STOR2/SEC1/etc — see Section 1 one-liners; fix directions:
SEC1  toast.js:96 innerHTML sink fed runtime strings at SettingsScreen.jsx:444,
  491-851. No untrusted substring reaches it TODAY (verified) but it's the exact
  stored-XSS shape. FIX: switch informational toasts to showToast({text});
  reserve {html} for static markup (SW update prompt's <b>).
BLD1  minify-bundle.mjs:44 / build.py:240. No gate proves minified corpus ==
  source values (PF1 proof was one-time/manual). FIX: vitest loading each
  src/data corpus + its built+minified dist bundle in two vm contexts,
  deepStrictEqual the globals.
BLD2  deploy-web.yml:34-101 has no `needs: build`; BLD-1 gate omits test/lint/
  typecheck/smoke/byte-identity. FIX: add `needs: build` or expand the gate.
CORP1 validate-schemas.js (~143/472/764). No dup-id detection (the likeliest
  regression). FIX: per-file Set of ids, ERROR on collision.
CORP2 validate-schemas.js:1564-1567. The 7 bible-*.js get only contiguity
  WARNINGS. FIX: validateAgainstReference vs BIBLE_KJV with a known-omission
  allow-list seeded from today's gaps.
ANN1  annotation-engine.jsx:238-250 (React) vs :346-414 (DOM). FIX: pass the
  group's globally-last (hlKey,segIdx) so only the final verse paints the icon.
ANN2  SelectionToolbar.jsx:413-419. removeHighlight bulk-removes every overlapping
  group incl. NoteStore.remove. FIX: exclude noted groups, or disclose in the
  confirm ("Remove 1 highlight and 1 note?").
NAV2  LetterView.jsx:30-33 / WtlbEntryView.jsx:21-23 peek + conditional clear.
  FIX: navHandoff.take() + re-stash on id mismatch, or clear unconditionally on
  reader mount.
NAV3  use-search.js:264-267 no-deps effect, unconditional null cleanup. FIX: []
  deps + restore prev (match __closeSheet convention).
APP2  use-scroll-memory.js:164-188. Scroll restore in a passive effect -> wrong-
  position flash. FIX: useLayoutEffect (writes scrollTop only).
PERF3 use-scroll-memory.js:119-123 + screen-routes.jsx (no React.memo). Scroll
  flush re-renders the whole verse list ~8x/s. FIX: React.memo heavy reading
  screens; subsumed partly by PERF4.
PERF4 app.css absent. FIX: content-visibility:auto + contain-intrinsic-size on
  .verse / .letter-para (DOM-preserving; keeps scroll-to-verse + annotation).
TEST1 utils/tabs.js + utils/highlight.jsx at 0% in scope. FIX: pure unit tests
  (tab->key strings; text+needle->segments incl. diacritic/case).
TEST2 react 19.2.6 tests vs 18.2.0 runtime. FIX: align test React to 18.2 OR a
  CI smoke under the runtime build; at minimum document.
SRCH2 search.js:1411-1417 skips trimming when terms<=4. FIX: short-circuit to
  no-results when the filtered content-term set is all stop-words.
BAK2  backup-android.js:178 skips the size cross-check when meta.size missing
  (web throws). FIX: treat missing/non-numeric size as corruption (match BAK-2).
BLD3  pre-commit:245,272-274 fail open on dist-only / app.jsx-only stages. FIX:
  always run check:app-size; refuse a dist/ stage with no source change.
CORP3 check_balance.py:68-81 flags only line-start left-double-quote. FIX: scan
  any smart quote outside string content, or document as esprima-redundant.

(P3 items: fix directions are the Section 1 one-liners; adjudicate ANN3/STOR4/
NAV6/APP6 during execution — several may be WONT-DO-with-reason.)

--------------------------------------------------------------------------------
SECTION 4 — WHAT'S GENUINELY GOOD (don't touch)
--------------------------------------------------------------------------------
- Journal background-kill durability: synchronous LS draft + immediate media
  persist + timestamp+content-signature reconcile; tests advance no timers.
- Security: CSP hash-locked (no unsafe-inline/eval), zero dangerouslySetInnerHTML
  in app code, WebView locked down, proto-pollution refused, provably no egress.
- Backup codec: 64-bit length-prefixed framing byte-identical web<->Kotlin,
  asserted from both sides; fail-loud export; fail-safe media replace.
- Annotation dual-render per-character equivalence test.
- Storage rebase-not-clobber hydration; IDB tx settle on oncomplete.
- Build cross-platform byte-identity engineered (CR-strip hashers + eol=lf).
- Search ranking (OR-union + AND-recall + coverage/phrase boost) proven non-
  vacuously; dual-engine isolation; cold-open loads all corpora before init.

--------------------------------------------------------------------------------
PROGRESS LOG
--------------------------------------------------------------------------------
2026-06-07c  AUDIT FULLY CLOSED — the 3 items held in 2026-06-07b are now resolved
            after the owner cleared them (no budget device; "as long as it's well-
            optimized we're fine; not an intensive app"):
            - ERR3 DONE (0b5c64e): instead of the originally-sketched <AppShellChrome>
              extraction (which would have touched app.jsx, at 798/800), the cleaner
              fix — AppShellOverlays + AppShellSheets each wrap their OWN return in
              <ErrorBoundary fallback={null}>. ErrorBoundary gained an optional
              `fallback` (omitted = the existing root panel; null = the crashed chrome
              vanishes + still logs). A crashed sheet/overlay no longer nukes the app;
              the screen + rest keep working. NO app.jsx change, no reorder. +3 tests;
              smoke:ci green (12 screens, annotation round-trips, 0 console errors).
            - NTV1 DONE (e8d6b50): restoreAudioModeIfActive() — restores
              vm.previousAudioMode only when mode is still MODE_IN_COMMUNICATION —
              called from onRenderProcessGone() + onDestroy() (teardown paths where no
              live recording exists, so it can't disturb a capture; NOT onPause).
              Kotlin compiles + :app:testDebugUnitTest green. Device-confirm optional.
            - PERF1 ADJUDICATED (won't-do, owner call): cold-boot win is mainly for
              slow devices the owner doesn't have; app isn't render-intensive; it's the
              riskiest remaining change (cold-boot path) → net-negative for a marginal
              gain. Plan retained in the SESSION HANDOFF if a budget audience appears.
            Net: every fleet-audit finding is now DONE or ADJUDICATED-with-reason.
            Optional owner-side: device confirms for NTV3 + NTV1; standing device walks.

2026-06-07b  TAIL CLOSE-OUT (session 2) — the ~49 remaining items swept. Method:
            7 per-cluster agents re-derived every open finding against CURRENT
            source (gotcha #1), I verified + executed. 13 DONE (8 commits, pushed),
            ~30 ADJUDICATED-with-reason, 3 held for you. All gates green (vitest +
            lint + typecheck + gradle :app:testDebugUnitTest; PERF4 preview-proven).

            DONE (commit):
            - BLD1 (f8a046e): vitest gate — evals each shipped minified lazy corpus
              bundle (a-bible/matthew/vot) + its concatenated source in two vm realms
              and deep-compares every global, so a value-corrupting minify fails CI.
            - BAK3+BAK5+SEC2 (8aa7b8e): verifyImportCounts reconciles the manifest's
              `counts` block vs what landed (surfaced in the import summary); the web
              FS-Access export sink gained abort() (no truncated .votbak on a failed
              write); the 3 import error toasts stopped leaking raw JSON.parse text
              (generic message + console.warn).
            - ANN4+ANN5 (f4909fc): applyNoteIcons normalizes [data-hl-dom] containers
              (heals splitText fragmentation now the A4 highlights-skip broke the
              incidental heal); +RED-without-fix test; +2 elementsFromPoint multi-note
              winner tests (the plural API was untested).
            - TEST3+TEST5 (a58ca42): 5 monotonic-ts assertions moved off real
              setTimeout(r,5) to a mocked Date.now (the prune test is now STRONGER);
              +1 test for the no-navigator.locks merge fallback (blind write + a
              once-per-store STOR5 trace) — every other cross-tab test rides the shim.
            - CORP6+BLD2 (e5c51f5): audit-footnotes.js gained --check (exit 1 on any
              dup/white footnote verse number) + a CI step (corpus clean: 0); the
              deploy publish gate widened to lint+typecheck+vitest+footnotes (it runs
              as a separate workflow from ci.yml, so it must re-prove the gates).
            - NTV3 (d2d5765): "Clear All My Data" now wipes the native Garden image
              disk cache (cacheDir/garden, ≤800 MB, survived the wipe before) via a
              new clearGardenCache bridge method, plumbed end-to-end + gated by
              BridgeContractTest + AppInterfaceTest + platform-bridge.test (34 keys).
              Device-verify owed: confirm the dir is empty after Clear-All.
            - PERF4 (e58eec5): content-visibility:auto + contain-intrinsic-size:auto
              on .section-block + .letter-para — DOM-preserving off-screen layout/paint
              skip. Preview-proven on Psalm 119 (176 verses): scroll-to-verse reaches
              v176, gold sups/headings intact, 0 console errors. Device scroll-feel owed.
            - SRCH4 (a119891): result snippets now highlight the matched SYNONYM
              (search "shepherd" → a "pastor" verse marks "pastor"), via a pure
              SearchScreen-only expandSnippetTerms over the exposed SYNONYM_MAP. Cross-
              translation spelling variants stay unhighlighted (no map; rare).

            ADJUDICATED — won't-do / already-handled / INFO (re-derived + proven):
            - ALREADY-DONE/SUBSUMED: SW3 (bible-*.js now corpusFirst via SW1); NTV2
              (finishV3Export(false) already deletes the partial SAF file via
              DocumentsContract); TEST2 (skew documented in VENDORED-LIBS.md AND
              exercised — smoke-ci boots the real 18.2.0 built bundle headless).
            - NET-NEGATIVE / CRITICAL-PATH risk: PERF3 (React.memo on reading screens
              never hits — 35+ fresh-identity callback props/render; redirected to the
              now-done PERF4); STOR4 (the _saveMerged structuredClone is the merge
              ANCESTOR — aliasing it reintroduces the STORE-1 clobber); APP5 (vot-state
              write is already debounced + async off the paint path; per-field decomp
              is explicitly rejected in state-store); SRCH5/SRCH6 (engine min-length +
              dead-code on the search critical path — UI already gates, removal is
              net-negative); BLD4 (package-lock + npm ci already pin esbuild exactly).
            - FALSE-PREMISE / LATENT-ONLY / BY-DESIGN: JRNL1 (each media insert flushes
              immediately — two can't share a render); JRNL2 (error-stage pendingSaveRef
              is never consumed); JRNL3 (totalEntries is a cumulative counter by design,
              clamped, milestones write-once); JRNL4 (the ts>=updated guard is already
              documented 4×); SCR2/SCR3 (comma-list/cross-chapter mis-resolves are real
              in parseRefStr but UNREACHABLE — every corpus ref is dict-keyed or a
              display-only cross-ref); SCR4 (book-only null is by design); APP3 (dwell
              reads dwellMs at call time, self-correcting); NAV5 (real but a tab-history
              SCHEMA redesign — accepted UX rough-edge); CORP4 (0 real ?. in any scanned
              data file); CORP5 (validateChain's prev/next pins ordering harder; num
              gaps are legit); ANN3 (O(n²) only on the single-block letter path, n tiny).
            - BENIGN INFO: BLD3 (CI git-diff is the dist/app.jsx backstop); BLD5; STOR3
              (one comment at most); SW4 (CORE_CACHE self-heals; the "persist()" premise
              is wrong but storage-health HAS a persist wrapper); APP4/APP6; NAV6
              (unreachable); NTV4 (intentional forgiving no-op, Robolectric-pinned);
              NTV5/NTV6 (defensive, unreachable); CORP3 (esprima is authoritative);
              TEST4 (compressImage error branches need a real raster pipeline jsdom lacks).

            HELD FOR YOU (open):
            - PERF1 — focused build-pipeline session + device cold-boot (see above).
            - NTV1 — device. EXACT SAFE PATCH: add a private MainActivity helper
              `restoreAudioModeIfActive()` = `audioManager?.let { if (it.mode ==
              AudioManager.MODE_IN_COMMUNICATION) try { it.mode = vm.previousAudioMode }
              catch (_:Exception){} }` and call it at the top of onRenderProcessGone()
              and in onDestroy(). Guarded on mode + only fires on teardown where the JS
              endAudioSession() can't, so it can't break a live recording. (Deliberately
              NOT onPause — a backgrounded recording is ambiguous; add after a device
              test if wanted.) Then watch the emulator: routing returns to speaker after
              a mid-record renderer-crash / app-finish.
            - ERR3 — focused render-tree extraction (see SESSION HANDOFF). app.jsx is
              798/800; the <AppShellChrome> wrapper that gives chrome its own
              ErrorBoundary also frees canary room. Verify via smoke:ci + preview.

2026-06-07  NAV4 landed (commit pending): useSwipeNav fired prev/next on any
            horizontal drag (only an active selection suppressed it), so a drag
            begun on a link / footnote-ref / annotation icon flipped the chapter
            underneath. onTouchStart now ignores the gesture when e.target.closest
            finds a tappable ancestor (a/button/input/.fn-ref/.inline-scrip-ref/the
            verse-link/bookmark/note icons); plain text + highlights still swipe.
            +2 hook tests; 8 swipe-nav tests green.
2026-06-07  BAK4 landed (commit pending): the v3-aware web import capped a legacy
            (v1/v2 JSON) whole-file .text() read at 300 MB while Android caps at 50
            MB (MAX_IMPORT_SIZE) and the older web path at WEB_MAX_IMPORT_BYTES (50
            MB). Lowered to 50 MB to match both (a 50-300 MB legacy JSON would OOM
            on a budget device); v3 streaming stays uncapped. Lint+typecheck clean.
2026-06-07  SRCH3 landed (commit pending, test-only): the engine stamps doc.volumeId
            from VotSearchData.VOLUME_COLLECTIONS[].id; use-search routes via
            COL_BY_SEARCH_ID (scripture-resolution) — two hand-kept registries.
            Added a cross-registry assertion (every VOLUME_COLLECTIONS id resolves
            in COL_BY_SEARCH_ID) so a future drift fails the gate instead of
            dead-ending a collection's results on tap. 43 search-engine tests green.
2026-06-07  SRCH2 landed (commit pending): executeSearch kept stop-words for any
            <=4-word query, so a pure-grammar-glue query ("the", "of the") matched
            ~every verse + built/sorted tens of thousands of hits (heaviest path)
            for a useless result. Now short-circuits to empty BEFORE index work when
            every term is a trimmed stop-word (and stop-word filtering is on); a
            query with ANY content word is untouched. +2 tests; 108 search tests +
            build + smoke:ci (critical-path bundle-a, 0 console errors) green.
2026-06-07  APP2 landed (commit pending): use-scroll-memory Effect 3 (scroll restore
            on nav) was a PASSIVE useEffect → ran after paint → the new screen
            flashed at scroll 0 for a frame. Made it useLayoutEffect (runs before
            paint). Safe + effective because __scrollEl is a ScreenLayout ref
            CALLBACK (attached during commit, before this parent layout effect);
            client-only, no SSR caveat. Sync write + rAF unchanged. 2092 tests pass.
2026-06-07  NAV2 landed (commit pending): only LetterView + WtlbEntryView consume
            the navHandoff 'pendingHighlight' slot (grep-verified), but the two set
            sites (goToLetterFromMatthew, openInAppLetter) set it for EVERY dest
            incl. a study chapter (no reader) — so it lingered until the next
            goHome/back, and a later letter with a colliding id would pulse a stale
            highlight. Gated the set on !dest.isStudy (study dest clears instead).
            +2 tests; 15 tap-through tests green.
2026-06-07  CORP2 landed (commit pending): new validateTranslationCompleteness(map,
            BIBLE_KJV) — presence-only — runs on each of the 7 alt-translations in
            the Format E section and ERRORs on a book/chapter the KJV reference has
            but the translation lacks. validateTranslationMap only checks the
            translation's OWN chapters, so a wholly-missing chapter was invisible;
            now-offline-cached (SW1) corpus must not ship a hole. Single-verse
            critical-text omissions stay contiguity WARNINGS (no allow-list needed).
            Live corpus still 1589 items / 0 errors (bookId scheme aligns w/ KJV —
            verified, no false-positives). +5 tests; 186 validate-schemas green.
2026-06-07  ANN1 landed (commit pending): on the React path HighlightableText runs
            per-verse, so noteIconsBySeg (from the verse-local lastSegByGroup)
            painted one note icon PER spanned verse for a multi-verse note (the DOM
            path paints one). Gated the icon on note.keys (the anchors, document-
            ordered): paint only when this verse is the group's LAST key. Single-
            verse/keys-less notes always paint (unchanged). +2 tests; 48 annotation-
            engine tests green.
2026-06-07  ANN2 landed (commit pending): the "Remove highlight" ✕ calls
            NoteStore.remove() per overlapping group — silently deleting the note
            BODY — while the confirm only said "Remove this highlight?". Now the
            confirm discloses it ("...and its note? The note text will be deleted.")
            when any overlapping highlight is noted. Extracted a shared
            selectionGroups() helper used by both removeHighlight (delete) and the
            note-count (disclose) so they can't drift; removeHighlight behavior
            unchanged. removeHighlight was UNTESTED — added the safety net: +3 tests
            driving the real toolbar→✕→confirm flow (noted warns, un-noted plain,
            confirm removes highlight AND note); 16 SelectionToolbar tests green.
2026-06-07  STOR2 + NAV3 ADJUDICATED (audit false-positives, re-derived + proven):
            - STOR2: the agent's premise (user "Clear All" routes through a merge-
              store clear() leaving residue) is WRONG for this app. The user-facing
              clearAllPersonalData (SettingsScreen.jsx:887) DELETES the whole IDB
              databases via _deleteIdbDatabase (votreader + vot-journal-media + 2
              caches) — no merge path. The reachable per-feature clear (Clear
              History → HistoryStore.clear) is a NON-merge store ({idb:true}, no
              crossTabMerge) → blind write, no residue. The merge stores' clear()
              (where the residue exists) have ZERO production callers (grep-proven).
              So the residue path is unreachable. No fix needed.
            - NAV3: the __goSearch no-deps re-bind is INTENTIONAL + documented (it
              re-binds each render so goSearch captures the latest nav state, like
              __onDwellCommit). The agent's fix ([] deps) would FREEZE a stale
              closure (a real bug); [goSearch] is equivalent to no-deps (goSearch is
              a fresh closure per render). Single owner → the "2nd-owner clobber" is
              moot; per-render churn is one property assignment. Net-negative to
              change.
2026-06-07  APP1 landed (commit pending) + PERF5 ADJUDICATED — Theme B half-closed.
            HighlightableText (the Bible/Matthew/Study React path) subscribed to
            NoteStore.getVersion() (whole-store) → a note edit re-rendered all 176
            verses. Now NoteStore set/update/remove/toggleNotebook bump only the
            per-hlKey versions the note anchors to (note.keys, via _bumpKeys), and
            HighlightableText subscribes getVersionForKey(hlKey). _bumpKey still
            bumps the GLOBAL version so the imperative DOM note-icon pass
            (letters/wtlb) still wakes; bulk ops keep global _bump. +3 NoteStore
            tests; engine mocks gained getVersionForKey; 72 tests green.
            PERF5 ADJUDICATED (won't-do): LinkIcon/BookmarkIcon match by KEY PREFIX
            (letter/wtlb selection-range suffixes), so an endpoint key
            "letter:x:2:10-40" differs from the icon's subscription key "letter:x:2"
            — exact-key versioning wouldn't wake the right icon. Mixing exact (bible)
            + prefix (letter) keying adds real complexity + render-path risk for a
            LOW-value, bounded fan-out (modest link/bookmark counts; getForKey is a
            cheap O(L) filter). Net-negative, like the dropped PF3.
2026-06-07  BAK2 landed (commit pending): v3AndroidImportEntries now throws on a
            manifest media entry with no numeric size (was silently accepted on
            Android while web's readContainer rejected it — a cross-platform
            integrity asymmetry on the only backup). +1 test; 57 backup tests pass.
2026-06-07  CORP1 landed (commit pending): validate-schemas Format A/B/D validators
            now track a per-file Set of seen ids and ERROR on a duplicate slug (the
            likeliest corpus regression — copy-paste a letter, forget the id; it
            misroutes nav/bookmarks/notes). Live corpus still 1589 items / 0 errors.
            +4 tests (A/B/D dup + a distinct-ids control); gated in pre-commit + CI.
2026-06-07  TEST1 landed (commit pending): +tabs.test.js (21) + highlight.test.jsx
            covering the previously-0%-covered load-bearing pure utils —
            tabContentKey/scrollKeyForTab (tab dedup + scroll-memory keys),
            describeTab/tabHasProgressBar, and normalizeForHighlight/
            splitWithHighlight/highlightExcerptInDom (search-result + excerpt
            highlighting, incl. smart-quote folding + the 8.4s auto-clear). Test-
            only, no source change. lint + typecheck clean.
2026-06-07  Hardening batch (commit pending): SEC1 + STOR5 + BAK6.
            - SEC1: SettingsScreen._showToast → showToast({text}) (textContent),
              not {html} (innerHTML); its ~30 callers pass runtime status strings,
              never trusted markup — closes the latent stored-XSS shape. No visible
              change (plain text renders identically).
            - STOR5: locks-unavailable merge fallback now traces once/store via
              DiagnosticLog (was silent). Unreachable on chrome108; sub-floor only.
            - BAK6: applyV3 streamedIds is now a Set, not a plain object (a media id
              of "__proto__"/"constructor" would make streamedIds[id] lie).
            145 backup+cached-store+cross-tab+toast tests pass; lint+typecheck clean.
2026-06-07  PERF2 landed (commit pending): JournalMediaStore._urlCache (id->blob:URL)
            was unbounded — only delete() freed an entry, so browsing years of
            photos/voice-memos pinned hundreds of MB of decoded blobs. Converted to
            an insertion-ordered Map LRU (cap 24): _cacheUrl evicts+revokes the LRU
            past the cap, _touchUrl bumps a hit to MRU; objectUrl re-creates on a
            later miss so eviction is invisible (blob stays in IDB). +1 test
            (30 puts → 6 oldest revoked); 23 store tests pass.
2026-06-07  SRCH1 landed (commit pending) — THEME A now fully CLOSED. search.js
            gains CORPUS_CONTENT_VERSION ('c9'), folded into dataSignature so a
            content-only corpus edit (same-length reword) busts the search cache
            (was purely structural — lengths/counts). check-corpus-version.js fails
            closed if CORPUS_CONTENT_VERSION != CORPUS_VERSION, so the corpus-edit
            → CORPUS_VERSION bump also rebuilds the index and they can't diverge.
            108 search tests + corpus gate + build + smoke:ci (0 console errors,
            critical-path bundle-a) all green.
2026-06-07  BAK1 landed (commit pending): applyV3 now streams media BEFORE applying
            stores. The media stream is the only step that can throw on a truncated
            Android container (frames read off the file; the `for await` itself
            throws, only the put is try-wrapped), so running it first means a
            truncation leaves stores as the OLD consistent data — making the
            "import failed" message accurate instead of a silent half-import on the
            user's only backup. Correct dependency order too (blobs before journal).
            Rejected the audit's "drain all frames up front" (would defeat streaming
            + reintroduce the GB-scale OOM v3 avoids). +1 test (mid-stream throw
            leaves stores UNAPPLIED — RED under old order); 72 backup tests pass.
            No SettingsScreen change needed — the existing message is now correct.
2026-06-07  NAV1 landed (commit pending): handleAndroidBack now consults
            modalRegistry FIRST (dismiss topmost, consume press) — mirroring the
            web Escape order — so Android hardware-back over a registered surface
            (NoteSheet, SelectionToolbar, the chip, bookmark/multi-note popovers,
            NotebookPickerSheet, JournalInboundSheet, ConfirmStrips) dismisses it
            instead of dismiss-AND-navigate. modalRegistry now IMPORTED (both files
            are bundle-b) not read as a bare global. BONUS: also fixes the web
            browser-BACK-button (popstate) path → all three dispatchers unified.
            +3 tests; 43 back-nav/modal-registry tests pass; lint+typecheck clean.
            FOLLOW-ON NAV1b (smaller, deferred): the inline Garden-warning /
            disable-tabs overlays still aren't registered (back navigates under
            them); the welcome splash is owner-OFF-LIMITS — do not touch.

2026-06-06  Fleet audit run (16 Opus agents). Plan authored. Execution started
            at Theme A (P0). 78 findings catalogued (6 HIGH, 1 MED-as-P0).
2026-06-06  THEME A core landed (commit pending): ERR1 + ERR2 + SW1 + SW2.
            - translations.js: loadBibleStudies resolves true/false + nulls its
              cached promise on failure (kills the permanent dead-end) + traces to
              DiagnosticLog; loadTranslation clears its cache slot on failure +
              traces (ERR2).
            - app.jsx: studiesError state + shared loadStudies (boot effect AND
              retry); +10 lines (canary 725->735, well under 800).
            - screen-routes.jsx + StudiesHome.jsx: "Try again" affordance; shows
              "Couldn't load studies." (empty) or "Some studies couldn't load."
              (partial — Matthew present, 7 studies failed).
            - service-worker.js: CORPUS_VERSION c8->c9; precache bible-studies.js +
              cache-on-use route for /src/data/bible-<code>.js (offline); corpusFirst
              now awaits the cache.put guarded (SW2).
            - check-corpus-version.js: gate now also fingerprints bible-studies.js +
              the 7 bible-<code>.js (globbed) so a content edit must bump
              CORPUS_VERSION (the new cache members can't go stale-forever).
            VERIFIED: 2044 vitest pass; smoke:ci 0 crashed/0 console.error; lint
            (max-warnings 0) + typecheck + csp + corpus-lock green; LIVE PREVIEW —
            forced a studies-load failure: retry affordance + DiagnosticLog trace
            rendered (vs old silent "coming soon"), then "Try again" recovered all
            7 studies + Matthew and cleared the error.
            REMAINING in Theme A: SRCH1 (search cache CORPUS_VERSION), SCR1
            (translation-bundle allowlist).
2026-06-06  SCR1 landed (commit pending): translations.js loadTranslation now
            allowlists `code` against TRANSLATION_OPTIONS (the page's single source
            of truth for shipped translations) so an unbundled tag (CJB/GNT/ESV/…)
            skips the doomed 404 + silent-NKJV-under-foreign-cite, jsdom-guarded.
            GOTCHA noted: app.jsx canary is now 798/800 (the Theme-A studiesError/
            loadStudies wiring filled it). PowerShell `Measure-Object -Line`
            UNDERCOUNTS — trust `wc -l` / check-app-size (split('\n').length-1).
            Any further app.jsx-touching item must EXTRACT first (e.g. fold the
            studies-load concern into useBibleStudies to reclaim ~13 lines).
2026-06-06  STOR1 landed (commit pending): mergeMapByKey gains a `subMerge` mode
            (used only by mergeAnnotationsStore) that merges annotation buckets
            member-wise over the UNION of keys with absent sides coerced to empty —
            so emptying a bucket (deleting the last segment → key removed) honors
            the member-delete instead of resurrecting the whole sibling bucket.
            Empty buckets pruned. Flat (notes) + union (journal-index) maps keep
            key-presence semantics. Localized to store-merge.js (no annotation-store
            change). +2 tests (resurrection repro was RED pre-fix); 136 store tests
            green; lint + typecheck clean.
            ── SRCH1 SCOPED (not a rushed edit): search.js dataSignature keys on
            SCHEMA_VERSION + structural lengths only, so a same-length reword serves
            a stale cached index forever. The page has NO CORPUS_VERSION global
            (it lives only in service-worker.js). Correct fix = expose CORPUS_VERSION
            to the page (build-injected) + a sync gate (extend check-corpus-version
            to assert page==SW), then fold it into dataSignature. Designed next.
