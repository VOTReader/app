# journal-6

Verdict: needs device.

The finding requires a mid-recording phone-call/microphone interruption and
Android `MediaRecorder`/audio-focus behavior. The only ADB target available
during this run was a physical Pixel 9 Pro over TLS ADB; `emulator` is not
installed and no headless AVD is available. No device was driven.
